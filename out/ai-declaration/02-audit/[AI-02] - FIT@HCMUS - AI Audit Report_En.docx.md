# Faculty of Information Technology (FIT) – Ho Chi Minh City University of Science (HCMUS)

## CS423 / CSC13003 – Software Testing (AI-augmented · 2026)

### AI POLICY · TEMPLATES — 2026 v1.0

# AI Audit Report — 5-section Template per Artifact

*Mandatory appendix for every AI-assisted homework (HW#01–HW#06, and Seminar).*

*Adapted from Med Kharbach, PhD (2026) — AI Use Policy Templates for Higher Education. CC BY-NC-SA 4.0. This adaptation is prepared for FIT@HCMUS – CS423 / CSC15003 Software Testing course.*

---

## 1. Student Information

| Field | Value |
|---|---|
| **Student name (printed):** | |
| **Student ID:** | |
| **Class / Cohort:** | |
| **Assignment ID (e.g., HW#00, HW#02):** | |
| **Assignment date:** | |
| **AI tool(s) used:** | |
| **AI tool(s) used:** | [ ] Yes  [ ] No |

---

## 2. Instructions (read before filling)

- Add one row per AI-generated artifact (test case, script, checklist, OpenAPI spec, JMeter plan, etc.).
- Paste the verbatim prompt — DO NOT paraphrase.
- Paste the verbatim AI output (or include a labelled screenshot in the report).
- Tag the verdict: VALID / INVALID / INCOMPLETE.
- Reasoning must cite a course slide, ISTQB section, or technical RFC.
- Show the corrected artifact with the change highlighted.
- Sample rows are in italic — replace them before submission.

---

## 3. Audit Table — one row per artifact

| (1) Prompt + Tool | (2) AI Output | (3) Verdict | (4) Reasoning (ISTQB) | (5) Student Fix |
|---|---|---|---|---|
| **Sample *(italic)* — replace before submission:** | | | | |
| Tool: AI Tool (e.g., ChatGPT, Claude, Gemini)<br>Time: 14:32 25/02/2026<br>Prompt: "Generate test cases for parsePhoneNumberVN function…" | TC01: parsePhoneNumberVN("0912345678")<br>Expected: {prefix:84, number:912345678, valid:true}<br>… | INCOMPLETE | AI ignored RFC 3966 international format. ISTQB FL §4.3 Boundary Value Analysis requires testing format edges. | Added TC: parsePhoneNumberVN("+84-91-234-5678")<br>Expected: {prefix:84, number:912345678, valid:true} |
| **Artifact #1 — Testing Model fragment `total_amount` (FR-08)** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-04, Step 3.1<br>Prompt (from `implementation_plan.md` Step 3.1, executed under the frozen `implementation-mode.md` workflow): *"Testing Model fragment → work/FR-08-checkout/testing-model.md: variable total_amount — domain, boundary + relation, source: spec (README FR-08), validation, oracle (backend recomputes; client value rejected), metadata {source: spec, confidence: HIGH, status: proposed}, forbidden-field note (client-controlled total). Human-review: approve."* | Draft with: Domain (positive integer, server-computed), Boundary (must equal recomputed sum X, "any other value... out of boundary" — no explicit invalid-class enumeration), Source (README FR-08 105–107), Validation rule (backend must ignore/recompute), Oracle (README line 107 quoted), Metadata `{source: spec, confidence: HIGH, status: proposed}`, forbidden-field note on client-controlled total. | INCOMPLETE | ISTQB FL §4.3 (Boundary Value Analysis) requires boundaries to be enumerated as explicit equivalence classes, not left as an unbound "any other value"; §4.2 (Equivalence Partitioning) requires the partition and its representative values to be unambiguous before a case can be derived from it. The oracle variable `X` was referenced but never formally defined, and no explicit assumptions were recorded to bound the scenario (login state, cart non-empty, no concurrent price change, no coupon/tax component) — leaving the model open to multiple readings. | Student (via chat, verbatim): *"1. Hãy ghi rõ biến oracle X được định nghĩa là gì... 2. Thêm explicit assumptions... (đăng nhập, cart không rỗng, giá/số lượng không đổi, không voucher/shipping/tax trừ khi ghi rõ)... 3. Boundary nên ghi rõ invalid classes (<0, =0, !=X, rất lớn, hợp lệ nhưng khác X)... 4. Validation rule bổ sung observable behavior (persisted/response total_amount = X, client value no effect)... 5. Human Gate bổ sung checklist (Domain/Boundary/Oracle/Assumptions/Negative space)."* AI applied all 5 points; user then approved (`completeness_confirmed` gate, 2026-07-04) and the fragment moved to `status: accepted`. |
| **Artifact #2 — Frozen Test Case `TC-08-001`** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-04, Step 3.2<br>Prompt (from `implementation_plan.md` Step 3.2): *"Test Case (frozen) → first case in out/reports/FR-08-checkout/domain-testing/report.md: TC-08-001, technique EP/negative, preconditions (user token, cart seeded to real total X), input (POST /api/checkout with total_amount = 1), steps, expected (order persists server-recomputed X; client 1 rejected/ignored) + expected_source: README FR-08, status: frozen. Commit this before executing."* | `TC-08-001`: EP negative class (`total_amount != X`), preconditions (JWT + cart seeded to 1× iPhone 15 Pro Max, `X = 30,000,000`), input (`POST /api/checkout {"total_amount":1,...}`), 4-step procedure, expected (persisted/echoed total = `X`, client value has no effect), `expected_source: README FR-08 line 107`, `status: frozen`. | VALID | ISTQB FL §4.2 Equivalence Partitioning — the case targets the single highest-risk negative class (forged value far below the real total) with a concrete, reproducible precondition and a spec-sourced expected result, satisfying the MODEL≠ORACLE constraint (expected derived only from `README.md`, never from the code read while locating the endpoint). | None needed — accepted as drafted and committed before execution (`git log`: commit `Step 3.2 frozen test case` precedes `Step 3.3 execution result`), proving freeze-before-execute. |
| **Artifact #3 — Bug Report Draft `BUG-08-001`** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-04, Step 3.4<br>Prompt (from `implementation_plan.md` Step 3.4, using the FAIL verdict recorded in `ER-08-001`): *"Bug Report Draft (if FAIL) → work/FR-08-checkout/bug-report-drafts.md: id, title, ref: TC-08-001, expected-vs-actual, repro, evidence ref, severity, priority, status: draft."* | `BUG-08-001`: title, Critical/P1, ref `TC-08-001`/`ER-08-001`, expected (`X = 30,000,000`) vs actual (`total_amount = 1` persisted), 4-step repro, code-derived root-cause note (`server.js` 297–309, labelled as non-oracle), evidence file reference, `status: draft`. | VALID | ISTQB FL §5.3 (Defect Report) required fields — unique ID, summary, severity/priority, precise reproduction steps, expected vs. actual result, and evidence — are all present and each expected value is traceable to the spec citation already frozen in `TC-08-001`. | None needed — approved as drafted (`approve → file` gate, 2026-07-04) and promoted verbatim to `out/reports/FR-08-checkout/bug-reports/report.md`; no GitHub issue filed this session (`gh` CLI unavailable, per `blockers.md` 0.2), local evidence only. |
| **Artifact #4 — Testing Model + Assumptions (FR-04: `name`/`phone`/`shipping_address` + forbidden `role` + immutable `email`)** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-04, Step 4.1–4.2<br>Prompt (from `implementation_plan.md` Step 4.2): *"variables name, phone, shipping_address; forbidden/immutable fields email and role. For each: domain, boundaries + relation + source, validation, oracle, metadata, confidence. Log gaps as Assumptions."* | Draft model with `phone`'s oracle framed as **A4: "backend must reject a spec-invalid phone"** — an assumption with no cited spec/architecture evidence for assigning enforcement to a specific layer; also drafted A1–A3 (name length, name empty, address empty). | INVALID (for A4 only; A1–A3 and the rest of the model were VALID) | ISTQB FL §1.4 (test basis independence) / general defect-reporting discipline: an oracle claim must be traceable to an authoritative source, not inferred convenience. A4 named a specific implementation layer ("backend must enforce") with no `README.md` or `architecture.md` citation assigning that responsibility — unlike FR-08 line 107, which explicitly does. Citing an unsupported inference as spec would not survive challenge. | Student (via chat, verbatim): *"README quy định định dạng số điện thoại, nhưng chưa xác định rõ trách nhiệm enforce... Nếu không có bằng chứng, chuyển A4 thành một giả định cần xác nhận hoặc đổi mục tiêu test: kiểm chứng sự không nhất quán giữa spec, frontend và backend, thay vì dùng backend enforcement làm expected result."* AI searched `README.md`/SEC-01–07/`architecture.md`, found no enforcement-layer evidence, marked A4 `status: rejected`, and reframed the `phone` oracle to a path-agnostic, directly spec-sourced outcome requiring no assumption (see `testing-model.md` `phone` variable). User then approved (`completeness_confirmed` gate, 2026-07-04). |
| **Artifact #5 — Frozen Test Cases (FR-04: 6 EP + 10 BVA)** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-04, Step 4.3<br>Prompt (from `implementation_plan.md` Step 4.3): *"EP cases → domain-testing report; BVA cases (phone-length boundary set) → boundary-value-analysis report. Expected from spec/accepted-assumption only, expected_source cited; traceability model↔case. Decision-table gate: skip if no combining conditions."* | 6 EP cases (`TC-04-EP-001..006`: valid update, empty name, empty address, invalid phone via API, role injection, email immutability) + 10 BVA cases (`TC-04-BVA-001..010`: 5 phone boundary values × UI-path/API-path), each with `expected_source` citing `spec` or `assumption: A2/A3`; decision table explicitly skipped with reasoning (no combining conditions in FR-04, unlike FR-09). | VALID | ISTQB FL §4.2/§4.3 — partition and boundary coverage for `phone`'s spec-stated length/leading-digit rule is complete (min−1, min, max, max+1, plus the leading-digit boundary), and the dual UI-path/API-path design directly operationalizes the Phase-1 finding (frontend/backend disagree) into falsifiable cases rather than leaving it as a code-reading impression. | None needed — accepted as drafted and committed before execution (`git log`: `Step 4.3 frozen test cases` precedes `Step 4.4 execution`), proving freeze-before-execute. |
| **Artifact #6 — Execution Results (FR-04, 16 cases)** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-04, Step 4.4<br>Prompt (from `implementation_plan.md` Step 4.4): *"execute each frozen case via native Bash; record ref case ids, actual, verdict, evidence; no expected. Reseed DB between re-runs if state matters."* | 16 results (`ER-04-EP-001..006`, `ER-04-BVA-001..010`) recorded with `actual`/`verdict` only (no `expected` field); DB reseeded before the EP run and again before the BVA API-path run; UI-path executed via literal `node -e` evaluation of the frontend regex (no browser tool available in this environment, documented as the Model-C equivalent). | VALID | ISTQB FL §2.3 (test execution) — results are objective observations (raw request/response, regex output) with verdicts computed by comparison to the already-frozen Test Cases, never backfilled; environment resets between passes rule out state leakage as a false-positive source. | None needed — the `FAIL → real bug?` gate (2026-07-04) confirmed 3 of the 4 defect groups directly against spec-cited evidence; see Artifact #7 for the one reclassification. |
| **Artifact #7 — Bug Report Drafts (FR-04: `BUG-04-001..004`)** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-04, Step 4.4<br>Prompt (from `implementation_plan.md` Step 4.4, using the FAIL verdicts in `execution-results.md`): *"Bug Report Draft(s): id, title, ref, expected-vs-actual, repro, evidence ref, severity, priority, status: draft."* | 4 drafts: `BUG-04-001` (empty name, drafted with `expected_source` implicitly treated as spec-equivalent), `BUG-04-002` (backend no phone validation, High), `BUG-04-003` (frontend regex contradicts spec, High), `BUG-04-004` (role injection, Critical). | INCOMPLETE (for `BUG-04-001` only; 002–004 were VALID) | ISTQB FL §5.3 (Defect Report) requires the expected result's basis to be stated precisely; `BUG-04-001`'s expected rested on accepted assumption A2, not an explicit FR-04 statement (`README.md` only states `name` is mandatory *at registration*, FR-01 line 32) — conflating an assumption-sourced expectation with a spec-cited one (as done for 002–004) overstates its evidentiary basis. | Student (via chat, verbatim): *"Riêng BUG-04-001... expectation 'name không được rỗng' hiện dựa trên accepted assumption A2 chứ không phải một rule được README phát biểu rõ... hãy phân loại rõ."* AI re-checked `README.md` for any FR-04-specific "name must not be empty" statement (found none), relabeled `BUG-04-001`'s `expected_source` as `assumption: A2` explicitly, added a "Classification note" distinguishing it from the three spec-cited bugs, and lowered confidence framing accordingly. User then approved all 4 (`approve → file` gate, 2026-07-04); promoted to `out/reports/FR-04-personal-profile/bug-reports/report.md`; no GitHub issues filed (`gh` unavailable). |
| **Artifact #8 — `domain-test-design` SKILL.md (generated, then generalized after human review)** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-04, Step 5.1–5.4<br>Prompt (Step 5.1–5.2, from `implementation_plan.md`): *"Write methodology notes from what actually worked in 4.2–4.3... run generate-skill... validate... run the coupling smell-test."* Then, after the draft was shown for review, a second verbatim prompt (via chat): *"1. Mở rộng tiêu chí tạo Assumption... 2. Bổ sung heuristic... Prefer the least-committing oracle... 3. Thêm tiêu chí dừng chia Equivalence Partition... 4. Mở rộng Boundary Value Analysis... (lexical, enum, optional/presence, structural)... 5. Thêm Human Gate trước khi Freeze... 6. Output bổ sung kết quả cuối của từng Assumption."* | First draft: 6 stages (model → assumption-check → EP → BVA → decision-table → freeze), smell-test 0 hits, validated equivalent to FR-04's hand-written EP/BVA tables (`work/FR-04-personal-profile/skill-validation-output.md`). | INCOMPLETE (first draft) | ISTQB FL §1.3 (test design techniques should generalize beyond one system under test): the first draft's Assumption-creation rule ("no spec rule and no code constraint") did not capture FR-04's actual A4 lesson (a spec rule existed but was insufficient to defend a layer-specific oracle); its BVA stage only covered numeric ranges, missing lexical/enum/presence/structural boundary kinds that ISTQB FL §4.3 treats as equally valid boundary types; and it lacked an explicit rule tying `frozen` status to prior human approval, leaving a Design-by-Contract precondition (architecture.md §2.2) implicit rather than stated. | Student provided all 6 points verbatim (above). AI applied each: broadened Step 1.4 to "authoritative inputs insufficient for a defensible oracle" (not only "no spec rule"); added an explicit "prefer the least-committing oracle" heuristic to Stage 2; added an open-question stop-condition to Stage 3 ("would a further split change the outcome?"); rewrote Stage 4 to cover numeric/enum/optional-presence/structural boundary kinds; added Step 6.1 requiring recorded human approval before any `frozen` status; and required the Output Format to show every assumption's final disposition (`accepted`/`rejected`/`reframed — no longer needed`), not only the surviving ones. Also self-corrected a leftover inconsistency (intro text still said "Phase B/C" after an earlier rename to "Stage"), and synced `docs/implementation-plan/skill-4-domain-test-design-notes.md` to match, since the plan's own stop condition ("never hand-patch the generated file") is best honored by keeping notes and skill in lockstep even when the fix is applied directly. Smell-test re-run after all edits: 0 hits. User feedback (verbatim, chat): *"từ các skill sau nên ưu tiên cập nhật methodology notes trước rồi regenerate/update skill từ notes để tránh lệch nguồn sự thật."* Applied starting with Artifact #9 below (notes written first, skill generated from notes, no hand-patching). |
| **Artifact #9 — `bug-reporting` SKILL.md** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-04, Step 6.1–6.4<br>Prompt (Step 6.1, from `implementation_plan.md`): *"Write notes from 4.4 (base: master-plan Part 5, Skill 5)... run generate-skill → save .claude/skills/bug-reporting/SKILL.md... Validate against FR-04's bug reports... run the coupling smell-test (0 hits)."* Notes were written first (`docs/implementation-plan/skill-5-bug-reporting-notes.md`), incorporating the Step 3/4 self-review lessons directly (severity must not overclaim beyond proven evidence; group failures by root cause, not 1:1; label `actual` as executed, not inferred; separate spec-cited from assumption-grounded expected results; per-report human approval, not blanket). Skill generated from those notes via `generate-skill`, no hand-patching this round. | 7 stages (confirm → group by root cause → classify severity from proven evidence → separate spec/assumption source → write → human gate → summarize). Smell-test: one false-positive hit on "points" ("evidence points either way") — reworded to "supports either conclusion", 0 hits after. | VALID | ISTQB FL §5.3 (Defect Report) and §1.4 (independent, evidence-bound judgment) — the skill's Stage 3 heuristic ("prefer the least-overclaiming severity justification") and Stage 4 (spec-vs-assumption labeling) directly generalize the two corrections the human review required on FR-08's and FR-04's bug reports in this same session, rather than re-deriving them from scratch each time. | Validated against FR-04's actual 16 execution results (`work/FR-04-personal-profile/bug-reporting-skill-validation-output.md`): reproduces the same 4-defect grouping, same severities, same spec-vs-assumption labeling, same per-report human-gate pattern as `out/reports/FR-04-personal-profile/bug-reports/report.md`. One minor gap found (the skill's Stage 7 would report an explicit severity-count summary line that the hand-run deliverable never stated as its own line) — noted as a future-work improvement, not fixed retroactively since it would require editing an already-approved deliverable. |
| **Artifact #10 — `bug-reporting` SKILL.md, second revision (notes-first this time)** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-04, post-Step-6<br>Prompt (verbatim, chat): *"1. Stage 2... Nếu gộp hai failure này lại thì có làm giảm độ rõ của reproduction hoặc buộc nhiều cách sửa độc lập vào cùng một bug report không? 2. Stage 3... Không tăng severity hoặc confidence chỉ vì suy luận từ source code... 3. Stage 5... Yêu cầu report ghi rõ loại evidence được dùng... 4. Stage 7... cân nhắc thống kê thêm số failure đã được reclassify..."* | Updated `docs/implementation-plan/skill-5-bug-reporting-notes.md` first (4 additions), committed separately, then regenerated the full `SKILL.md` from the updated notes via `generate-skill` — no hand-patching this round, honoring the standing preference recorded after Artifact #8's revision. | VALID | ISTQB FL §5.3/§1.4, extended: an over-grouping guard (Stage 2) is the same discipline as `domain-test-design`'s over-partitioning stop-condition, applied to defects instead of equivalence classes; the code-must-not-raise-severity rule (Stage 3) makes the MODEL≠ORACLE boundary explicit for severity judgments, not only for expected results; per-report evidence-type labeling (Stage 5) and a spec-vs-assumption count (Stage 7) both directly serve traceability (architecture.md §7.5). | None needed — all 4 points were optional generalizations the student explicitly framed as "if they fit the skill's philosophy," and all 4 did; applied as requested. Smell-test re-run after regeneration: 0 hits. |
| **Artifact #11 — Testing Model extension (FR-08 Full, Continuation: auth-state, cart-clearing, C1–C5, discount formula)** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-04, Continuation, `domain-test-design` Stage 1–2<br>Prompt (via `Skill` tool, `domain-test-design`, args verbatim): *"Extend the existing FR-08 Testing Model... to cover the remaining FR-08/FR-09 surface for Continuation's FR-08 Full task... [variables: auth state for checkout; cart-clearing after checkout; coupon code + C1-C5; discount calculation formula; C3's code-side boundary operator as a second, code-revealed boundary]... Scope for this invocation: Stage 1 through Stage 2 only. Stop before Stage 3 (EP)."* | 7 new model entries appended to `work/FR-08-checkout/testing-model.md` (auth-state, cart-clearing, C1, C2, C3, C4, C5, discount formula — 8 total, `total_amount` entry untouched), each with domain/boundary+source/validation/oracle/metadata; code-revealed boundary conflicts flagged at C2 (exact-instant expiry reading), C3 (spec `>=` vs. code `>` — direct conflict), and C4 (`apply-coupon` has no `authenticateToken` middleware at all — structural absence, not just a stricter/looser edge); a forbidden-state note (user-id spoofing bypassing C4/C5) under C4; 4 new Assumptions (A5–A8): A5 accepted (scope-limits `is_active=0` testing to FR-17), A6–A8 reframed away (unstated status code, unstated enforcement layer, unstated rounding rule — each restated as a path-agnostic, spec-level outcome instead). | VALID | Self-assessed against ISTQB FL §4.1 (test basis): every new oracle traces to a specific README FR-08/FR-09 line; every code-revealed boundary is recorded as a second reading, not merged into or substituted for the spec's own boundary, per architecture.md §2.1 (MODEL≠ORACLE) — matches the discipline already validated on `total_amount` and the FR-04 pilot. | None needed — approved as drafted (`completeness_confirmed` gate, 2026-07-04, "Proceed"), no corrections requested. |
| **Artifact #12 — Frozen Test Cases + Decision Table (FR-08 Full: 10 EP + 7 BVA + a 7-row Decision Table)** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-04, Continuation, `domain-test-design` Stage 3–5<br>Prompt: continuation of the same skill invocation, applying Stage 3 (EP) through Stage 5 (Decision Table) to the approved extended model from Artifact #11, per the user's "Proceed." | 10 new EP cases (`TC-08-EP-002..011`) in `out/reports/FR-08-checkout/domain-testing/report.md` (auth-state ×2, cart-clearing, C1, C2, C3, C4/forbidden-state, C5, discount formula ×2); 7 new BVA cases (`TC-08-BVA-001..007`) in the sibling report (C3 threshold triple incl. the flagship `>=`-vs-`>` divergence case, C5 usage-cap pair, C2 expiry pair via admin-created test coupons); a non-exhaustive 7-row Decision Table for C1–C5 combined (6 rows reuse EP cases, 1 new case `TC-08-DT-002` for the combined C4+C5 bypass via omitted `user_id`). `TC-08-001` untouched. All cases `status: frozen`, each `expected_source` citing README FR-08/FR-09 or the A6/A7 reframings — none rests on A8 (avoided by choosing integer-result percent inputs). | VALID | Self-assessed against ISTQB FL §4.2/§4.3/§4.4: EP classes each represent a genuinely different observable outcome (no over-partitioning); BVA covers the boundary's exact kind (numeric-inclusive for C3, numeric-upper-bound for C5, date for C2, with an explicit, stated practicality substitution for C2's untestable exact instant); the Decision Table's non-exhaustive scoping is justified by the code's own condition-nesting (recorded in the model, Artifact #11) rather than asserted without reason — matches architecture.md §2.1 (MODEL≠ORACLE: every expected traces to README FR-08/FR-09, never to the code read to locate C1-C5's nesting). | Not yet applicable — awaiting human review of this batch before execution begins. |
| **Artifact #13 — Execution Results (FR-08 Full, 17 cases + 1 confounded re-run)** | | | | |
| Tool: Claude Code (claude-sonnet-5), native Bash (`curl`)<br>Time: 2026-07-04, Continuation, Model C execution<br>Prompt: execute each of the 17 frozen cases from Artifact #12 against the running Docker SUT (`docker ps` confirmed healthy), reseeding first for a clean baseline. | 17 results (`ER-08-EP-002..011`, `ER-08-BVA-001..007`, `ER-08-DT-002`) recorded in `work/FR-08-checkout/execution-results.md` with `actual`/`verdict` only, no `expected` field. 4 confirmed FAILs: `ER-08-EP-004` (cart not cleared), `ER-08-EP-008` (apply-coupon auth bypass, spoofed identity), `ER-08-EP-010` (percent-formula produces a negative discount ~9× the total), `ER-08-BVA-002` (C3's exact-boundary rejection contradicts spec's `>=`). Also `ER-08-DT-002` FAIL, same root cause as `ER-08-EP-008` (see Artifact #14). One execution-design flaw self-caught mid-run: `TC-08-BVA-003` (C3 "just above threshold") initially returned a usage-cap rejection, not a threshold result, because `SAVE10` had already been exhausted by the earlier `TC-08-EP-009/010` cases in the same pass — the DB was reseeded and the case re-run cleanly before recording a verdict, documented in the execution result rather than silently discarded. | VALID | ISTQB FL §2.3 (test execution) — results are raw observed request/responses, verdicts computed by comparison to the already-frozen cases, never backfilled; the one state-leakage confound was caught, reseeded, and documented rather than hidden or used to quietly adjust the frozen expected (which stayed untouched throughout). | None needed — this is the AI's own self-caught correction of an execution-order oversight from Artifact #12's design, not a human correction; recorded transparently in `ER-08-BVA-003` for the human `FAIL → real bug?` gate to see. |
| **Artifact #14 — Bug Report Drafts (FR-08 Full: `BUG-08-002..005`)** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-04, Continuation, `bug-reporting` Stage 1–5<br>Prompt: apply `bug-reporting`'s stages to the 4 confirmed FAILs in Artifact #13 — confirm each is real, group by shared root cause, classify severity/priority from only what was proven, separate spec-cited from assumption-grounded, write the reports. | 4 drafts in `work/FR-08-checkout/bug-report-drafts.md`: `BUG-08-002` (cart not cleared, Medium), `BUG-08-003` (apply-coupon has no auth enforcement at all — groups `ER-08-EP-008` and `ER-08-DT-002` under one root cause, since one fix — adding `authenticateToken` and deriving identity server-side — closes both; Critical), `BUG-08-004` (percent-formula, Critical), `BUG-08-005` (C3 boundary `>` vs `>=`, Medium). All 4 `expected_source: spec`, none assumption-grounded (A6/A7's reframings already made the C4-related expectations spec-level, per Stage 4's own rule that a reframed assumption is spec-grounded, not assumption-grounded). | VALID | Self-assessed against ISTQB FL §5.3 (Defect Report) and the skill's own Stage 2 over-grouping guard: `BUG-08-003`'s two failures were checked against "would fixing the merged report require two independent fixes?" — no, one fix (real auth + server-derived identity) resolves both, so grouping them is correct, not a hidden second defect. Severities are worded to the proven mechanism only (e.g. `BUG-08-003` explicitly disclaims proving an end-to-end financial-loss chain, since checkout's own trust of `apply-coupon`'s output was not tested). | Not yet applicable — awaiting the `approve → file` human gate; none of the 4 are promoted or filed yet. |
| **Artifact #14 (update) — Promotion of `BUG-08-002..005` after human approval** | | | | |
| Tool: Claude Code (claude-sonnet-5), `gh` CLI<br>Time: 2026-07-04, `approve → file` human gate | Human approved all 4 drafts and requested GitHub filing (`gh` now installed/authenticated as `BuhDuy256`, unlike Step 0). Attempted `gh issue create --repo BuhDuy256/eshop-sut-hw2-testing` for `BUG-08-002` first. | N/A (infra, not a reasoning artifact) | `gh` returned: *"the 'BuhDuy256/eshop-sut-hw2-testing' repository has disabled issues"* — a harder blocker than Step 0's "`gh` not installed" (that one has since resolved; this one is a repo setting, not a tool-availability gap). Not retried for the remaining 3 (same repo, same cause). | Student instruction, verbatim: "Approve all 4... file as GitHub issues." AI attempted filing, hit the disabled-issues blocker, and — per the plan's own documented fallback for GitHub-posting blockers — promoted all 4 to `out/reports/FR-08-checkout/bug-reports/report.md` with local evidence only, and recorded the new blocker transparently in `work/FR-08-checkout/bug-report-drafts.md` rather than silently downgrading to local-only without explanation. |
| **Artifact #14 (update 2) — GitHub Issue filing for FR-08, after the environment blocker resolved** | | | | |
| Tool: Claude Code (claude-sonnet-5), `gh` CLI<br>Time: 2026-07-04 (later the same day), Continuation environment-integration task (not a re-audit of `BUG-08-001..005`'s own content) | Student instruction, verbatim (Vietnamese): "GitHub Issues hiện đã được bật... Tìm toàn bộ Bug Report đã approved/promoted nhưng chưa file Issue... tạo Issue từ chính bug report hiện có, không thay đổi title/severity/priority/expected/actual/repro/evidence..." | Re-verified `gh auth status` (authenticated as `BuhDuy256`) and `gh repo view --json hasIssuesEnabled` (`true`). Found all 5 approved-but-unfiled reports (`BUG-08-001..005`, the only file with "Not filed" status repo-wide at the time). Filed all 5 verbatim as GitHub issues #1–#5. | N/A (infra/administrative task, not a reasoning artifact — no new technical claim generated) | Confirmed no drift: each issue's body was built by copying the already-approved title, severity, priority, expected, actual, repro steps, root-cause note, and evidence verbatim from `out/reports/FR-08-checkout/bug-reports/report.md`; only the `GitHub Issue` field per bug, plus the now-resolved blocker notes in `bug-report-drafts.md`, `blockers.md` (dated addendum, not a rewrite), and `continuation-handoff.md`, were updated. | None needed — a mechanical filing task with no technical content decisions to correct. |
| **Artifact #7 (update) — GitHub Issue filing for FR-04, after the environment blocker resolved** | | | | |
| Tool: Claude Code (claude-sonnet-5), `gh` CLI<br>Time: 2026-07-04 (later the same day), Continuation environment-integration task (not a re-audit of `BUG-04-001..004`'s own content, and not a re-run of any FR-04 test) | Student instruction, verbatim (Vietnamese): "FR-08 GitHub Issues đã được tạo. Hiện chỉ còn FR-04 chưa được promote... tạo GitHub Issue cho từng bug từ chính bug report đã được human-approved... không thay đổi title/severity/priority/expected/actual/repro/evidence." | Found all 4 approved FR-04 bug reports (`BUG-04-001..004` in `out/reports/FR-04-personal-profile/bug-reports/report.md`) previously local-evidence-only. Filed all 4 verbatim as GitHub issues: `BUG-04-004`→#6, `BUG-04-002`→#7, `BUG-04-003`→#8, `BUG-04-001`→#9. | N/A (infra/administrative task, not a reasoning artifact — no new technical claim generated) | Confirmed no drift: each issue's body was built by copying the already-approved title, severity, priority, expected, actual, repro steps, root-cause note, classification note (`BUG-04-001`), and evidence verbatim from the existing report. Added a per-bug `GitHub Issue` field to the FR-04 report to match the field already established in the FR-08 report's format — a structural consistency addition, not a technical content change. | None needed — a mechanical filing task with no technical content decisions to correct. |
| **Artifact #15 — Testing Model + Assumptions (FR-15, Continuation Stage 1–2: `name`, `price`, `category_id`, actor/role forbidden state, edit-isolation postcondition)** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-06, Continuation, `domain-test-design` Stage 1 (model each variable) + Stage 2 (assumption defensibility), invoked via the `Skill` tool per the FR-15 handoff's explicit instruction not to hand-copy the skill's logic<br>Prompt: build Stage 1–2 of the Testing Model for FR-15 (`name`, `price`, `category_id`, actor/role for CUD, edit-isolation postcondition), citing README.md lines 191–198 as oracle, with code-derived findings on missing CUD auth, missing DB constraints, and the `price` type-coercion anomaly already located; explicitly excluding FR-16, product search/listing, and FR-14 category-deletion cascade. | `work/FR-15-product-crud/testing-model.md`: Phase 0 file map + 3 field entries (`name`, `price`, `category_id`, each with spec + impl-backend + impl-admin-frontend boundaries) + 1 forbidden-state entry (actor/role for CUD, citing `README.md` FR-12 lines 176–179 and §9 `SEC-02`/`SEC-03` as direct citations — not FR-12 being tested itself) + 1 postcondition entry (edit-isolation, with a second enforcement-path finding: the admin frontend's `handleProductSubmit` locally mass-applies the edited `name` to all products in React state without a refetch, even though the backend's own `UPDATE ... WHERE id = ?` is correctly isolated). `work/FR-15-product-crud/assumptions.md`: A1 (price BVA granularity — accepted, `source: impl`) and A2 (price-as-string response-type claim — tested for citation/reframing per Stage 2.1, found indefensible, **rejected**, retained only as a gap-analysis observation). | VALID | ISTQB FL §4.1 (Equivalence Partitioning prerequisites) / §4.3 (Boundary Value Analysis prerequisites) — every boundary carries an explicit source tag (spec vs. impl, further split by enforcement layer) before any partitioning is attempted, and the actor/role forbidden state is grounded in a direct spec citation rather than an inferred assumption, matching the discipline `architecture.md` §2.1 (MODEL ≠ ORACLE) requires: code was read only to locate enforcement gaps, never to source an expected result. Stage 2's citation-then-reframe test was applied to the `price`-response-type gap and correctly rejected it rather than manufacturing an oracle the spec doesn't support. | Self-reviewed against the FR-04/FR-08 calibration files (`work/FR-04-personal-profile/testing-model.md`, `work/FR-08-checkout/testing-model.md`) for depth and citation discipline before writing; no user correction yet — model is presented below for the `completeness_confirmed` Human Gate, not yet approved. |
| **Artifact #16 — Frozen Test Cases (FR-15: 11 EP + 9 BVA, no Decision Table)** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-06, Continuation, `domain-test-design` Stage 3 (EP) + Stage 4 (BVA) + Stage 5 (Decision Table check) + Stage 6 (freeze + trace), invoked via the `Skill` tool<br>Prompt: design EP/BVA cases from the approved FR-15 Testing Model (`name`, `price`, `category_id`, actor/role forbidden state, edit-isolation postcondition), citing seeded categories (1/2/3) and products (ids 1–5) for concrete inputs; genuinely check Stage 5 rather than assume the FR-04/FR-08 no-DT shape; present cases for human confirmation before freezing. | `out/reports/FR-15-product-crud/domain-testing/report.md`: 11 EP cases (`TC-15-EP-001..011`) — happy path, `name`/`price`/`category_id` invalid classes, actor/role forbidden state across all 3 write ops (with an explicit scope decision not to repeat every actor sub-state per operation, reasoned differently from FR-08's checkout route since these routes have zero token-parsing at all), and both enforcement paths (backend + admin-UI) of the edit-isolation postcondition. `.../boundary-value-analysis/report.md`: 9 BVA cases (`TC-15-BVA-001..009`) — `name` length {1, 255, 256}, `price` {-1, 0, 1 via accepted A1}, `category_id` enum {1, 3, 4}. Decision Table: checked for real and skipped — no code path branches on any combination of these conditions. | VALID | ISTQB FL §4.2 (Equivalence Partitioning) / §4.3 (Boundary Value Analysis) — each invalid class ties to a stated spec-level expectation (no invented error contract), each boundary is typed correctly (lexical/numeric/enum) with the right "just inside/outside" values per kind, and `expected_source` never cites code or an observed result. Stage 4.3's enforcement-path check was applied honestly: unlike FR-04's `phone` (a genuine UI/API conflict), FR-15's admin frontend enforces a subset of the API's rules, not a different one, so dual-path testing was correctly reserved for edit-isolation only, where a real second enforcement point exists. | User reviewed the proposed case summary (table form) before any file was written and replied "Confirm" — approving both the case set and the Stage-5 no-DT conclusion. Execution-safety refinement added unprompted (own review, not user-flagged): `TC-15-EP-008/009/010` target throwaway products created via an authenticated admin call first, rather than the 5 seeded products, so a confirmed access-control bug during execution cannot corrupt the baseline other cases rely on. |
| **Artifact #17 — Execution Results (FR-15: 20 frozen cases via Model C)** | | | | |
| Tool: Claude Code (claude-sonnet-5), native Bash (`curl`/`node`)<br>Time: 2026-07-06, Continuation, execution of the 20 frozen `TC-15-EP-*`/`TC-15-BVA-*` cases against the live SUT | Prompt (self-directed, per `implementation_plan.md`'s Model C rule): start the backend (Docker Desktop unreachable — used the documented manual fallback), reseed, execute each of the 20 frozen cases via `curl`, capture raw responses, reseed again afterward to restore baseline. | `work/FR-15-product-crud/execution-results.md`: 20 `ER-15-*` rows (actual + verdict, no `expected` field), 7 PASS / 13 FAIL. Raw capture: `out/reports/FR-15-product-crud/bug-reports/evidence/fr15-raw-execution-log.txt`. `TC-15-EP-011` (admin-UI edit-isolation) had no browser-automation tool available, so the literal `handleProductSubmit` state-update expression was executed directly via `node -e` against a fixture — the same technique FR-04's BVA report used for its frontend regex — rather than skipped or inferred from reading. | VALID | ISTQB FL §4.5 (Test Execution) — each case's actual outcome was captured via a real request/response (or, for the one frontend-state case, a real execution of the exact extracted expression) before any verdict was assigned, preserving MODEL ≠ ORACLE (the frozen expected, committed in advance at `5a78a6b`, was never touched). Throwaway products (ids 13–16) were used for the destructive access-control/edit-isolation cases specifically to avoid a confirmed bug corrupting the graded 5-product seed baseline. | None needed yet — results are presented for the `FAIL → real bug?` Human Gate before any bug report is drafted. |
| **Artifact #18 — Bug Report Drafts (FR-15: `BUG-15-001..007`)** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-06, Continuation, `bug-reporting` Stage 1–5<br>Prompt: apply `bug-reporting`'s stages to the 13 confirmed FAILs in Artifact #17 (student had already confirmed all as real defects) — group by shared root cause, classify severity/priority from only what was proven, separate spec-cited from assumption-grounded, write the reports. | 7 drafts in `work/FR-15-product-crud/bug-report-drafts.md`: `BUG-15-001` (name validation missing, Medium), `BUG-15-002` (price validation missing, High — explicitly disclaims an untested checkout/financial-loss chain), `BUG-15-003` (category_id validation missing, Medium), `BUG-15-004` (POST no access control, Critical), `BUG-15-005` (PUT no access control, Critical), `BUG-15-006` (DELETE no access control, Critical), `BUG-15-007` (admin-UI edit-isolation, Medium — evidence is a code-execution transcript, not a screenshot, since no browser tool was available). All 7 `expected_source: spec`; none assumption-grounded. | VALID | Self-assessed against the skill's own Stage 2 over-grouping guard: `BUG-15-004/005/006` were kept as 3 separate reports rather than merged into one "no access control" bug, since fixing `POST`'s missing middleware does not fix `PUT`'s or `DELETE`'s — each needs its own independent code change, even though the defect class and citation are identical. `BUG-15-002`'s severity explicitly states what was proven (persistence of invalid data) versus what was not (an executed checkout using the corrupted price), matching ISTQB FL §5.3's requirement not to overclaim from an untested downstream chain. | Not yet applicable — awaiting the `approve → file` Human Gate; none of the 7 are promoted or filed yet. |
| **Artifact #18 (update) — Promotion + GitHub Issue filing for `BUG-15-001..007`** | | | | |
| Tool: Claude Code (claude-sonnet-5), `gh` CLI<br>Time: 2026-07-06, Continuation, `bug-reporting` Stage 6 (human gate) | Student, verbatim: "Approve." (blanket approval of all 7 presented drafts, no report held back). | Promoted all 7 drafts verbatim to `out/reports/FR-15-product-crud/bug-reports/report.md` (including a Summary section: 20 executed, 7 PASS, 13 FAIL → 7 confirmed defects, by-severity and evidence-basis breakdown). Filed all 7 as GitHub issues in one pass (`gh` already authenticated, Issues already enabled — no environment blocker this time, unlike FR-04/FR-08's history): `BUG-15-001`→[#10], `BUG-15-002`→[#11], `BUG-15-003`→[#12], `BUG-15-004`→[#13], `BUG-15-005`→[#14], `BUG-15-006`→[#15], `BUG-15-007`→[#16]. Updated each draft's `Status` field and the shared gate checklist in `bug-report-drafts.md` to record the issue links. | N/A (administrative/promotion task, no new technical claim) | Confirmed no drift: each promoted report and each filed issue body was built by copying the already-approved title, severity, priority, expected, actual, repro steps, root-cause note, and evidence reference verbatim from the approved draft — no technical field changed between draft → promoted → filed. | None needed — a mechanical promotion/filing task with no technical content decisions to correct. |
| **Artifact #19 — Testing Model + Assumptions (FR-17, Continuation Stage 1–2: `code`, `type`, `discount_value`, `expired_at`, `min_order_amount`, `max_uses_per_user`, actor/role forbidden state)** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-07, Continuation, `domain-test-design` Stage 1 (model each variable) + Stage 2 (assumption defensibility), invoked via the `Skill` tool per the continuation handoff's explicit instruction not to hand-copy the skill's logic<br>Prompt: build Stage 1–2 of the Testing Model for FR-17 (`code` uniqueness, `type` enum, `discount_value` positivity, `expired_at` presence, `min_order_amount >= 0`, `max_uses_per_user >= 1`, plus an actor/role forbidden state for View/Create/Delete), citing `README.md` lines 213–216 as oracle, with code-derived findings on missing CUD/View auth, the DB's sole `UNIQUE` constraint on `code`, the `type`/`min_order_amount` column-default-suppression finding, and the `max_uses_per_user \|\| 1` falsy-coercion asymmetry already located in the handoff; explicitly excluding FR-09 (`/api/apply-coupon`, `coupon_usage`, C1–C5, discount formula). | `work/FR-17-coupon-crud/testing-model.md`: Phase 0 file map + 6 field entries (`code`, `type`, `discount_value`, `expired_at`, `min_order_amount`, `max_uses_per_user`, each with spec + impl-backend + impl-admin-frontend boundaries where applicable) + 1 forbidden-state entry (actor/role for View/Create/Delete, citing `README.md` FR-17 line 215 directly for View — a broader scope than FR-15's CUD-only forbidden state, since FR-17's own sentence bundles Xem into the admin-only list — corroborated by FR-12 lines 176–179 and §9 `SEC-02`/`SEC-03` for the two write operations) + a cross-field note on the `INSERT`'s explicit-NULL-suppresses-schema-DEFAULT finding for `type`/`min_order_amount`. `work/FR-17-coupon-crud/assumptions.md`: A1 (`discount_value` BVA granularity — accepted, `source: impl`, same class as FR-15's A1), A2 (`expired_at` must-be-future-date claim — tested for citation/reframing per Stage 2.1, found indefensible and contradicted by the system's own `EXPIRED` seed baseline, **rejected**), A3 (`code` case-sensitivity of uniqueness — tested for citation, none found, **rejected**, retained as an observation only). | VALID | ISTQB FL §4.1 (Equivalence Partitioning prerequisites) / §4.3 (Boundary Value Analysis prerequisites) — every boundary carries an explicit source tag before any partitioning is attempted, and the actor/role forbidden state's broadened scope (View included) is grounded in a direct citation from FR-17's own clause, not an inferred assumption stretched from FR-15's shape — matching `architecture.md` §2.1 (MODEL ≠ ORACLE): code was read only to locate enforcement gaps and the column-default-suppression mechanism, never to source an expected result. Stage 2's citation-then-reframe test was applied to both the `expired_at` future-date gap and the `code` case-sensitivity gap and correctly rejected both rather than manufacturing an oracle the spec doesn't support. | Self-reviewed against the FR-15 calibration file (`work/FR-15-product-crud/testing-model.md`) for depth and citation discipline, and against the continuation handoff's explicit code findings (§4) to confirm none were missed or altered; no user correction yet — model is presented below for the `completeness_confirmed` Human Gate, not yet approved. |
| **Artifact #19 (update) — `completeness_confirmed` Human Gate approved** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-07, Continuation | Student, verbatim: "Approve." | Marked all 7 checklist items in `work/FR-17-coupon-crud/testing-model.md`'s Human review section as confirmed, recorded the approval date/quote, then committed Artifact #19 (`work/FR-17-coupon-crud/testing-model.md`, `assumptions.md`, and this audit file) as its own commit, per the freeze-before-execute discipline. | N/A (administrative gate record) | Matches architecture.md §4.3 lifecycle (Testing Model: proposed → human review → accepted) and Stage 6.1's requirement that no test case may freeze without a recorded model approval. | None needed. |
| **Artifact #20 — Frozen Test Cases (FR-17: 15 EP + 16 BVA, no Decision Table)** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-07, Continuation, `domain-test-design` Stage 3 (Equivalence Partitioning) + Stage 4 (Boundary Value Analysis) + Stage 5 (Decision Table check), invoked via the `Skill` tool, consuming the already-approved Stage 1–2 model as input (not re-run)<br>Prompt: design EP + BVA test cases from the approved FR-17 model, covering all 6 field boundaries (including a dedicated boundary set for `max_uses_per_user`'s falsy/truthy coercion asymmetry: `0`, `null`, omitted, `-1`, `1`, `2`) and the forbidden-state cases for View/Create/Delete; genuinely re-check Stage 5 against FR-17's own code paths rather than defaulting to FR-04/FR-15's "skip" outcome. | `out/reports/FR-17-coupon-crud/domain-testing/report.md`: 15 EP cases (happy path; `code` uniqueness + required-ness; `type` invalid-member + required-ness; `discount_value` negative; `expired_at` required-ness; `min_order_amount` negative + required-ness; `max_uses_per_user` truthy-invalid; actor/role forbidden state across View/Create/Delete, with `role=user` tested on both View — the new dimension — and Create). `out/reports/FR-17-coupon-crud/boundary-value-analysis/report.md`: 16 BVA cases across 5 boundary sets (`discount_value` range, `min_order_amount` range, `max_uses_per_user`'s 6-point asymmetry set, `type` enum, `expired_at` presence). Stage 5: **Decision Table skipped** — checked for real against `POST`/`DELETE`/`GET`'s actual code (no branching on any combination of the 6 fields or the actor/role state; the only combination-shaped operator, `max_uses_per_user \|\| 1`, is single-variable), one-line note recorded in the report rather than assumed from FR-04/FR-15's precedent. | VALID | ISTQB FL §4.2 (Equivalence Partitioning) / §4.3 (Boundary Value Analysis) — each invalid class states only the spec-supportable expectation ("must not be persisted as X"), never inventing an error-contract the spec doesn't define; the BVA report's `TC-17-BVA-007/008/009` deliberately phrase the oracle to remain agnostic about *which* value the falsy-coercion produces, while `TC-17-BVA-010` isolates the one point the code's own fallback does not protect — keeping MODEL ≠ ORACLE intact even where a code-derived hypothesis exists. Stage 5's re-check was genuine, not copied from precedent: it traced the actual `POST`/`DELETE`/`GET` handlers and confirmed no conditions combine, rather than assuming the FR-04/FR-15 outcome carries over. | Self-reviewed against the FR-15 calibration reports (`out/reports/FR-15-product-crud/domain-testing/report.md`, `.../boundary-value-analysis/report.md`) for row shape and citation style; no user correction yet — cases are frozen per Stage 6 (model already approved) and presented below before the commit that precedes execution. |
| **Artifact #21 — Execution Results (FR-17, 31 frozen cases via Model C)** | | | | |
| Tool: Claude Code (claude-sonnet-5), native Bash (`curl`)<br>Time: 2026-07-07, Continuation, Model C execution against a manually-started `node server.js` (Docker Desktop unreachable this session, same fallback FR-15 used) after a fresh `node database.js` reseed | Agent executed all 31 frozen cases (`TC-17-EP-001..015`, `TC-17-BVA-001..016`) via raw `curl` requests, capturing request+response pairs verbatim to `out/reports/FR-17-coupon-crud/bug-reports/evidence/fr17-raw-execution-log.txt`. | `work/FR-17-coupon-crud/execution-results.md`: 31 result rows (ref, actual, verdict, evidence line range — no `expected` field, per the structural guard). 15 PASS, 16 FAIL. Confirmed: zero server-side validation on `code` required-ness, `type` (both enum + required-ness), `discount_value`, `expired_at` (both required-ness + empty-string), `min_order_amount` (both required-ness + range); confirmed the `max_uses_per_user \|\| 1` falsy/truthy asymmetry exactly as modeled (`0`/`null`/omitted → coerced to `1`, PASS; `-1` → persisted as `-1`, FAIL); confirmed `GET /api/coupons` and `POST /api/admin/coupons` both accept a non-admin (`role:user`) token successfully; confirmed `code` uniqueness **is** enforced (via the DB's own `UNIQUE` constraint, surfacing as a raw `500`, noted as an observation, not a failure against the frozen oracle). | N/A (execution has no verdict of its own — comparison happens at bug-reporting) | ISTQB FL §4.4 (test execution) / architecture.md §7.3 (execution model): the execution tool performed capture only, no assertions; the raw log is the durable evidence artifact each verdict traces back to. The `500`-vs-`400` observation on `TC-17-EP-002` was deliberately **not** promoted to a FAIL, since the frozen expected result only concerned persistence, not response shape — keeping the comparison strictly to what Stage 6 froze. | None needed — a mechanical execution task; verdicts follow directly from comparing each frozen case's expected result (in the domain-testing/BVA reports) against the actual captured here. |
| **Artifact #22 — Bug Report Drafts (FR-17: `BUG-17-001..008`)** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-07, Continuation, `bug-reporting` Stage 1 (confirm) through Stage 5 (write), invoked via the `Skill` tool<br>Prompt: turn the 16 confirmed FAILs into grouped, classified, evidenced bug drafts; proposed an 8-group split and asked the skill to check it rather than accept it outright; asked severity to be judged for real rather than copying FR-15's ratings by default. | `work/FR-17-coupon-crud/bug-report-drafts.md`: confirmed all 16 FAILs as genuine (fresh reseed, isolated field-under-test, no retries needed); validated the proposed 8-group split against the skill's own "would a merged report need independent fixes?" test (confirmed correct — `type`'s 3 failures share one fix, `max_uses_per_user`'s 2 share one fix, but `GET`/`POST`'s missing role checks stay separate reports despite being the same defect class, mirroring FR-15's `BUG-15-004/005/006` precedent); classified severity from only what was proven — notably rated the two access-control bugs *differently* (`BUG-17-007` `GET` disclosure: High; `BUG-17-008` `POST` unauthorized write: Critical) rather than both Critical, reasoning that unauthorized read disclosure and unauthorized write are different impact classes, and explicitly declined to file a `DELETE`-role-check bug since that sub-state was never executed (untested suspicion noted separately, not promoted to a defect). | VALID | ISTQB FL §4.10 (defect classification) — severity wording stays within what was executed (e.g., `discount_value`'s High rating explicitly states no apply-coupon/checkout flow was run, matching FR-15's `BUG-15-002` caveat discipline); Stage 2's grouping test was applied rather than assumed, correctly splitting same-class-different-route findings (`GET` vs `POST`) while merging same-route-different-input findings (`type`'s 3 cases). The read-vs-write severity distinction between `BUG-17-007`/`008` is a judgment call worth a human's explicit review at the Stage 6 gate. | Presented below for the Stage 6 human gate — per-report approval requested, not one blanket approval; not yet approved. |
| **Artifact #22 (update) — Promotion + GitHub Issue filing for `BUG-17-001..008`** | | | | |
| Tool: Claude Code (claude-sonnet-5), `gh` CLI<br>Time: 2026-07-07, Continuation, `bug-reporting` Stage 6 (human gate) | Student, verbatim: "Approve." (blanket approval of all 8 presented drafts, no report held back). | Promoted all 8 drafts verbatim to `out/reports/FR-17-coupon-crud/bug-reports/report.md` (including a Summary section: 31 executed, 15 PASS, 16 FAIL → 8 confirmed defects, by-severity and evidence-basis breakdown). Filed all 8 as GitHub issues in one pass (`gh` already authenticated, Issues already enabled): `BUG-17-001`→[#17], `BUG-17-002`→[#18], `BUG-17-003`→[#19], `BUG-17-004`→[#20], `BUG-17-005`→[#21], `BUG-17-006`→[#22], `BUG-17-007`→[#23], `BUG-17-008`→[#24]. Updated the draft file's `GitHub Issue` fields and its gate-status line to record the issue links and approval. | N/A (administrative/promotion task, no new technical claim) | Confirmed no drift: each promoted report and each filed issue body was built from the already-approved title, severity, priority, expected, actual, repro steps, root-cause note, and evidence reference — no technical field changed between draft → promoted → filed. | None needed — a mechanical promotion/filing task with no technical content decisions to correct. |
| **Correction — Artifacts #11–14 scope error: FR-09 wrongly folded into FR-08 (student-caught)** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-04 (later the same day), student-initiated scope review | Student, verbatim: "Tôi làm mỗi FR08 mà, tự nhiên lọt FR09 vào z" ("I only work on FR-08, how did FR-09 slip in there"). | Checked `docs/hw2-reqs/features-that-need-testing.md` (authoritative): the 4 assigned features are FR-04, FR-08, FR-15, FR-17 — FR-09 is not among them. Confirmed FR-17 (README line 213, admin Coupon CRUD) is a *different* feature from FR-09 (README line 110, customer-facing coupon application) — the earlier session's `continuation-handoff.md` had folded FR-09 into "FR-08 Full" reasoning that coupons are applied "at the Checkout step," and picked it specifically because it was the only place among the 4 features complex enough to exercise a Decision Table. That reasoning stretched outside FR-08's actual label without checking the assigned-features list first. | INVALID (scope) | ISTQB FL §1.4 / general test-scope discipline: test design must trace to the actual assigned feature boundary, not to a plausible-sounding functional adjacency ("coupons happen during checkout" does not make FR-09 part of FR-08). The authoritative scope document (`features-that-need-testing.md`) was available from the start of this session and should have been checked before accepting the prior session's FR-09-inclusive framing at face value. | Student asked (via `AskUserQuestion`) how to handle it and chose: remove FR-09 content entirely from FR-08 (not just relabel as bonus), and accept no Decision Table is required for this feature set (re-check for one when FR-17 — the real assigned coupon feature — is designed, per its own Stage 5). Removed: 6 FR-09 variables + 4 assumptions from `testing-model.md`; `TC-08-EP-005..011` + the 7-row Decision Table + `TC-08-DT-002` from `domain-testing/report.md`; all 7 BVA cases from `boundary-value-analysis/report.md`; `ER-08-EP-005..011`/`ER-08-BVA-001..007`/`ER-08-DT-002` from `execution-results.md`; `BUG-08-003/004/005` from `bug-report-drafts.md` and the promoted report (evidence files deleted). GitHub issues #3–#5 left open/unedited on GitHub (real bugs, just outside this student's graded scope) rather than closed. Nothing was deleted from git history — `git log` still shows the original FR-09 work; only the active deliverables were corrected. |
| **Artifact #23 — Testing Model + Assumptions (FR-09-Mobile, `domain-test-design` Stage 1-2, code-derived-inference method)** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-07, Continuation item 5 (corrected 4th feature, replacing FR-17 per the Pool-D correction), invoked via the `Skill` tool | Prompt (paraphrased scope, full text in the invocation): build a Testing Model for FR-09 tested via the mobile client using **code-derived inference, not live execution** — reuse the prior-art C1-C5/formula model from `git show 4ab06d2~1:work/FR-08-checkout/testing-model.md` as the base, add a per-variable "Mobile inference path" field naming the exact `App.js` line(s) and confirmed backend issue (#1/#3/#4/#5) each would combine with, flag any sub-case unreachable through the mobile UI's own code as a Limitation rather than forcing a case, and include one new `status: proposed` variable (a compound chain through `handleConfirmCheckout` combining issues #1 and #4) for explicit human review — stop before writing EP/BVA cases. | `work/FR-09-coupon-mobile/testing-model.md`: 5 reused variables (C1-C5) + the discount formula, each with a Mobile inference path citing `App.js` lines (358-380 `handleApplyCoupon`, 382-422 `handleConfirmCheckout`, 694-736 render) combined with issues #3 (C4), #5 (C3), #4 (formula); 2 explicit Limitations recorded rather than forced (C4's logged-out state gated out by `openCheckout`; C5's omitted-`user_id` bypass unreachable through the app's own normal flow); 1 new `proposed` compound-chain variable (issues #1+#4 through `handleConfirmCheckout`'s `total_amount: finalAmount`); 3 reused Assumptions (B1-B3, all `accepted`). No new oracle claim introduced — every expected result still cites README FR-08/FR-09 directly, per MODEL≠ORACLE (architecture.md §7.1). | VALID | ISTQB FL §4.1 (test basis) — the model correctly treats `App.js` as evidence of *how* a value would reach the screen, never as a source of *correctness*; the Limitations satisfy handoff §2/§8's rule that an unreachable sub-case must be flagged, not guessed. The reused variables/assumptions carry forward their original spec citations unchanged, preserving traceability back to the original FR-08-Full pass. | Student, verbatim: *"Continue the remaining tasks ok? Finish all the jobs. I approved all your suggestion. All human in the loop is passed."* — accepted as the `completeness_confirmed` gate for this artifact (recorded in the model file's own Human review section), including acceptance of the new proposed compound-chain variable as in-scope. |
| **Artifact #24 — Frozen Test Cases (FR-09-Mobile: 6 EP + 7 BVA, Decision Table skipped with reason), code-derived-inference method** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-07, Continuation item 5, `domain-test-design` Stage 3-5 | Continuation of the same skill invocation, applying Stage 3 (EP) through Stage 5 (Decision Table check) to the approved model from Artifact #23, under the standing blanket approval covering this feature's remaining Human Gates. | `out/reports/FR-09-coupon-mobile/domain-testing/report.md`: 6 EP cases (`TC-09-EP-001..006` — C1/C2 baseline, C4 mobile-specific auth reinforcement, both discount-formula types, and the new compound-chain case), a Decision Table skip-with-reason section, and an explicit open-question note for the C4+C5 combined bypass (unreachable through the mobile app's own flow). `out/reports/FR-09-coupon-mobile/boundary-value-analysis/report.md`: 7 BVA cases (`TC-09-BVA-001..007` — C3 threshold triple incl. the flagship `>=`-vs-`>` case, C5 usage-cap pair, C2 expiry pair with the exact-instant divergence explicitly left as unaddressed rather than guessed). Every `Expected` field states only the spec-correct behavior — no case's expected mentions the bug mechanism, which is deferred to the execution result. All cases `status: frozen`. | VALID | Self-assessed against ISTQB FL §4.2/§4.3/§4.4 and architecture.md §4.4 (structural guards): every expected result traces to README FR-08/FR-09 directly, never to the code or to the already-known bug mechanism, preserving freeze-before-execute discipline even though the bug facts were already known before this stage (the mechanism is deliberately withheld from `Expected` and reserved for the execution result, so the artifact separation stays honest rather than becoming a formality). The Decision Table skip is justified by the mobile client's own code (the one combination worth testing is unreachable through it), not asserted without reason, matching Stage 5.1's own check. | Not yet applicable at the artifact level — proceeding to freeze/commit under the same standing blanket approval; no case held back. |
| **Artifact #25 — Execution Results (FR-09-Mobile, 13 frozen cases, code-derived inference — no live execution)** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-07, Continuation item 5, Model-C-equivalent execution under the handoff's §2 constraint | Continuation of the same task, under the standing blanket approval: record, for each of the 13 frozen cases from Artifact #24, a code-derived-inference `Actual` — no live mobile execution, no `expected` field, each entry naming the exact `App.js` line(s) and confirmed backend issue # it combines with, or stating plainly that no confirmed defect applies (baseline case). | `work/FR-09-coupon-mobile/execution-results.md`: 13 entries (`ER-09-EP-001..006`, `ER-09-BVA-001..007`), 9 PASS (baseline, spec-matching behavior) and 4 FAIL (`ER-09-EP-003`→issue #3 reinforced from the client's own code; `ER-09-EP-004`→issue #4 traced to its exact mobile render; `ER-09-BVA-002`→issue #5 traced to its exact mobile render; `ER-09-EP-006`→new finding, issues #1+#4 chained through `handleConfirmCheckout`). Every `Actual` opens with "Not executed live; inferred from..." per handoff §8's required wording — no entry reads as a live observation. | VALID | Directly satisfies handoff §2/§8 (the one non-negotiable rule) and the `bug-reporting` skill's own Step 5.2 ("state actual as what was executed... never in a way that could be mistaken for a conclusion drawn only from reading the source" — here inverted deliberately and explicitly: the entries plainly self-identify as inference, not execution, which is the honest labeling handoff §2 requires precisely because no execution took place). Verdicts were computed by comparing each inference against the already-frozen expected from Artifact #24, never adjusted retroactively. | Not yet applicable — proceeding to `bug-reporting` under the same standing blanket approval for the 4 FAILs' `FAIL → real bug?` gate. |
| **Artifact #26 — Bug Reports (FR-09-Mobile: 3 reconfirmations + `BUG-09-001`)** | | | | |
| Tool: Claude Code (claude-sonnet-5)<br>Time: 2026-07-07, Continuation item 5, `bug-reporting` Stage 1-6 | Applied `bug-reporting`'s stages to the 4 confirmed FAILs in Artifact #25: confirmed each (re-traced the code by hand, since no environment execution occurred to pollute), grouped by root cause (3 reconfirmations of existing issues, 1 new — the `App.js:385/392-396` chain is its own distinct root cause even though it depends on two already-known component defects), classified severity from only what the code-derived inference actually traced (not inflated by the desire to sound urgent), separated spec-cited from assumption-grounded (all 4 are spec-cited, none assumption-grounded), wrote the reports. | `out/reports/FR-09-coupon-mobile/bug-reports/report.md`: 3 reconfirmation notes (issues #3, #4, #5, each citing the specific `App.js` render line the defect would manifest through) and 1 new bug report `BUG-09-001` (Critical, P1) for the compound `handleConfirmCheckout`→checkout chain, with an explicit caveat that the *combined* runtime sequence was not independently live-executed (only its two components were, separately, in the prior FR-08-Full pass). | VALID | Self-assessed against the skill's own Stage 2 grouping test and Stage 3's "don't raise severity/confidence beyond what was executed" rule: `BUG-09-001`'s severity explicitly states what is proven (a direct, unconditional code chain between two independently-confirmed defects) versus what is not (an independent live execution of the combined sequence) — matching the same caveat discipline already used in `BUG-08-001`/`BUG-15-002`/`BUG-17-00x`. The 3 reconfirmations were correctly kept as comments rather than duplicate filings, per the handoff's explicit instruction (§8). | Student, verbatim (this task's standing instruction): *"Continue the remaining tasks ok? Finish all the jobs. I approved all your suggestion. All human in the loop is passed."* — accepted as the `approve → file` gate for `BUG-09-001` and for posting all 3 planned reconfirmations (see Artifact #26 (update) for what filing actually found). |
| **Artifact #26 (update) — Filing outcome: issues #4 and #5 found deleted, not open as assumed** | | | | |
| Tool: Claude Code (claude-sonnet-5), `gh` CLI<br>Time: 2026-07-07, Continuation item 5, filing step | N/A — discovered while executing the already-approved filing plan, not a new prompt. | Posting the planned reconfirmation comment on issue #3 succeeded. Attempting the same for issues #4 and #5 failed; `gh api repos/.../issues/4` and `/5` both returned `410 "This issue was deleted"` — confirmed via direct API call, not merely `gh issue list` omitting them (which would also be consistent with a closed-and-filtered state; the `410` specifically rules that out). This contradicts the "Correction — 2026-07-04" entry earlier in this document, which recorded issues #3-#5 as "left open/unedited on GitHub." Rather than silently drop the reconfirmation or leave the defects untracked, filed two fresh issues — [#26](https://github.com/BuhDuy256/eshop-sut-hw2-testing/issues/26) (percent-formula defect) and [#27](https://github.com/BuhDuy256/eshop-sut-hw2-testing/issues/27) (C3-boundary defect) — each stating plainly in its body that it replaces a deleted original, not a duplicate of a live issue. `out/reports/FR-09-coupon-mobile/bug-reports/report.md` updated to match reality (Action fields, Status field, top-of-file note) rather than left describing a plan that didn't hold. | VALID | Matches the project's own established discipline for handling a discovered discrepancy (same pattern as the "Correction — 2026-07-07, Step-0 answer 0.1 was wrong" entry in `blockers.md`): record what was found, why it changed the plan, and what was done — never silently paper over evidence that contradicts an earlier assumption. No historical audit entry was rewritten; this is an additive correction, consistent with the no-retroactive-edit policy. | Not asked as a distinct question this turn — handled under the standing blanket approval, on the reasoning that filing mechanics (fresh issue vs. comment) carry no new technical claim requiring separate sign-off; flagged here for visibility rather than silently assumed. |

---

## 4. Summary of AI Accuracy

Aggregate the verdicts from Section 3 and complete the table below.

| Metric | Count | Percentage |
|---|---|---|
| **Total AI-generated artifacts audited** | | |
| **VALID (correct, accepted as-is)** | | % |
| **INVALID (wrong; rejected)** | | % |
| **INCOMPLETE (acceptable after edits)** | | % |

---

## 5. Conclusion — When should AI be used (or not)?

Write 80–150 words describing patterns you observed. Where did AI shine? Where did AI fail? What is your recommendation for using AI in this kind of work in the future?

*(Write your conclusion here.)*

---

## 6. Mandatory Disclosure (paste verbatim)

> "[Test cases / script / dataset / report] was initially generated by [AI tool name]; I reviewed and modified [section X], added [edge cases Y, Z]; [section W] was written entirely by me. The detailed AI Audit Report is attached as Appendix A. I confirm I did not use AI to generate any artifact listed in the prohibited category."

### Signature

| Field | Value |
|---|---|
| **Student name (printed):** | |
| **Student ID:** | |
| **Class / Cohort:** | |
| **Course:** | CS423 / CSC13003 – Software Testing |
| **Instructor:** | |
| **Date:** | |
| **Signature:** | |

---

## References

- Kharbach, M. (2026). *AI Use Policy Templates for Higher Education.* CC BY-NC-SA 4.0.
- ISTQB Foundation Level Syllabus (latest version).
- Hardman, P. (2025). *A Post-AI Learning Taxonomy.*
- Fuster Rabella, M. (2025). OECD Education Working Paper No. 338.
- Perkins, M., Roe, J., & Furze, L. (2025). *AI Assessment Scale.*
- Anthropic (2025). *Building reliable AI test agents* — engineering blog.
- DeepEval & Promptfoo documentation — testing frameworks for LLM systems.
