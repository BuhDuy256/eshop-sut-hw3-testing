# Checklist Execution — Screen C2 (Edit User dialog · Assign Role · Block/Unblock)

**Scenario:** C — Admin manages users
**Người thực hiện:** Nguyễn Bảo Duy — 23127179 — Group 09
**Checklist:** `hw3/docs/gui-checklist/Shared_GUI_Checklist.md` v1.9 — 60 items (frozen)
**SUT:** https://prod-dev.ems-fitus.cloud/ · account `admin@gmail.com`
**URL màn hình:** `https://prod-dev.ems-fitus.cloud/dashboard/admin/users` — **C2 là dialog, URL không đổi khi mở**
**Đường đi:** C1 → cột ACTIONS → nút icon `aria-label="Edit user"`
**Ngày execute:** 2026-08-02
**Browser:** Chrome (Windows 11) qua extension `claude-in-chrome`

---

> ### C2 gánh những gì — đọc trước
>
> Kết quả blocker **B-12** (chi tiết ở `../../../work/notes/C2-exploration.md` §5): dialog Edit User **chứa cả** ô sửa thông tin, dropdown **Assign Role**, **và** checkbox `Active` — tức là **cơ chế Block/Unblock cũng nằm ở đây**, không phải một dialog riêng. Vì vậy C2 gánh:
> - toàn bộ nhóm **IA-02 (Forms)** của scenario C — đây là form duy nhất trên bề mặt user-admin;
> - **Assign Role** (dropdown Role);
> - **Block / Unblock** (checkbox Active);
> - **IA04-02** (modality) và **IA03-10** (ESC) cho dialog không phá huỷ.
>
> Dialog **Create New User** (mở từ nút `Add User`) dùng **cùng một component form** với Edit User — cùng bộ label, cùng thứ tự field, chỉ khác ở chỗ có thêm field `Password` và nút ghi `Create User`. Các item về đánh dấu required và về label được kiểm trên **cả hai** dialog và cho **cùng** kết quả; ảnh bằng chứng chụp ở Create New User vì ở đó field còn rỗng nên nhìn thấy placeholder.
>
> **Một thao tác không thực hiện được:** tạo user mới thật. Form Create New User bắt buộc field `Password` (`required=true`), và người chạy phiên này không nhập mật khẩu vào bất kỳ ô nào. Không mất coverage: mọi item đều kiểm được bằng dialog **Edit** trên tài khoản test có sẵn `test abc`, cộng với việc submit form Create khi bỏ trống để xem validation.

**Dữ liệu dùng để thao tác ghi:** `test abc` / `abcdefabc@gmail.com` / Guest / Inactive — tài khoản test **có sẵn từ trước**, không thuộc 3 thành viên còn lại của nhóm, không phải `admin@gmail.com`. Đã Block→Unblock→Block round-trip; **trạng thái cuối trùng trạng thái đầu (Inactive)**.

---

## IA-01 — General UI Standards (13 items)

| Item ID | Nội dung (tóm tắt) | Verdict | Notes | Evidence |
| --- | --- | --- | --- | --- |
| IA01-01 | Consistency: page-title, padding card, canh KPI card | N/A | Dialog không có page-title, content card hay KPI card — những thứ item này so sánh. Đã chấm ở **C1**. | |
| IA01-02 | 9 icon sidebar cùng stroke/size, active state giống nhau | N/A | Sidebar không thuộc dialog (và bị overlay che khi dialog mở). Đã chấm ở **C1**. | |
| IA01-03 | Màu primary chỉ dùng cho CTA chính | Pass | Trong dialog có đúng **1** hành động chính và nó dùng màu cyan: `Save Changes` (Edit) / `Create User` (Create). `Cancel` để trắng viền xám, `X` không màu. Không thấy màu accent bị dùng trang trí ở chỗ khác trong dialog. | |
| IA01-04 | Section header khác biệt rõ với field label | N/A | Dialog Edit/Create User không chia section — chỉ có một dãy field phẳng, không có section header nào để so với field label. | |
| IA01-05 | Contrast ≥ 4.5:1 trên status pill | N/A | Không có status pill nào bên trong dialog; trạng thái ở đây là checkbox `Active`, không phải pill. Đã đo pill ở **C1**. | |
| IA01-06 | Empty state khi không có kết quả | N/A | Dialog không có danh sách nào có thể rỗng. | |
| IA01-07 | Loading state (Slow 3G), số KPI không nháy "0" | N/A | Item yêu cầu **reload một màn hình nhiều dữ liệu**; dialog không tự tải danh sách và không có số KPI. Ca cần DevTools throttling được để lại ở **C1**. | |
| IA01-08 | Toggle EN/VI dịch hết label, kể cả `<title>` | N/A | Control chuyển ngôn ngữ nằm ở header trang, không nằm trong dialog. Đã chấm ở **C1** (Pass — sinh viên kiểm lại thủ công, toggle hoạt động bình thường), không chấm lần thứ hai ở đây. | |
| IA01-09 | Định dạng ngày/số đổi theo locale | N/A | Dialog không hiển thị ngày, giờ hay số có định dạng theo locale (Member Code và Phone là chuỗi thô). | |
| IA01-10 | Spotlight hero · ⚠pred-N/A | N/A | Không có hero trong dialog. | |
| IA01-11 | QR/barcode · ⚠pred-N/A | N/A | Không có QR/barcode trong dialog. | |
| IA01-12 | Focus ring rõ khi Tab, thứ tự focus hợp lý | Pass | **Đo ngày 2026-08-03.** Mở dialog Edit User: focus chuyển vào trong dialog bình thường. Không có finding nào phát sinh từ item này trên C2. | |
| IA01-13 | Ảnh có `alt` mô tả | N/A | Dialog không chứa `<img>` nào. | |

## IA-02 — Forms (15 items)

| Item ID | Nội dung (tóm tắt) | Verdict | Notes | Evidence |
| --- | --- | --- | --- | --- |
| IA02-01 | Đánh dấu required: (a) nhìn thấy, (b) `required`/`aria-required`, (c) thực sự chặn | **Fail** | Kiểm tách 3 vế đúng như item yêu cầu. **(a) Nhìn thấy — KHÔNG CÓ GÌ.** `dialog.textContent.includes('*')` = `false`; kiểm thêm cả marker vẽ bằng CSS: `getComputedStyle(label,'::before').content` và `::after` đều trả `none` trên **cả 8** label. Không dấu sao, không chữ "required", không đổi màu. **(b) Programmatic — CÓ `required`, KHÔNG CÓ `aria-required`.** Edit User: First Name / Last Name / Email = `required=true`; Create User thêm Password. Nhưng `aria-required` = `null` trên **toàn bộ** input. Nặng hơn: **`<label>` không liên kết với input** — cả 7 `<label>` đều `for=null`, và `input.labels` rỗng với mọi field trừ checkbox `Active`. **(c) Thực sự chặn — CÓ.** Submit rỗng bị chặn (xem IA02-09). **Kết luận:** ba vế lệch nhau — hệ thống *bắt buộc* 4 field, *không nói* cho người nhìn biết field nào bắt buộc, và *không phơi* thông tin đó cho screen reader. Người dùng chỉ phát hiện ra field nào bắt buộc bằng cách submit hỏng. Expected của item còn ghi "Different conventions between forms is a Fail": form Create Support Request (khảo sát nhóm) có 3 `required`, form Add/Edit Event có **0** `required` dù vẽ dấu sao đỏ — ba form, ba quy ước. | evidence/C2/C2_IA02-01_no-required-marker-and-swapped-labels.png |
| IA02-02 | Label không biến mất khi gõ (không phải placeholder-only) | **Fail** | Vế "label có ở lại khi gõ không" thì **đạt** — label là text riêng phía trên ô, không phải placeholder-only. Nhưng phát hiện một lỗi nặng hơn nhiều ở chính chức năng mà label phải làm: **hai label `First Name` và `Last Name` bị gán ngược so với field mà chúng đứng trên**. Xác nhận bằng **ba nguồn độc lập, tất cả đồng ý với nhau và cùng chống lại label**: (1) **placeholder** — ô nằm dưới label `First Name` có placeholder `Last Name`, và ngược lại; (2) **thông báo lỗi validation** — bỏ trống rồi submit, ô dưới label `First Name` báo **"Last name is required"**, ô dưới label `Last Name` báo **"First name is required"**; (3) **file Excel do chính EMS xuất ra** — với user hiển thị là `SƠN BÙI QUANG`, file ghi `Last Name = "BÙI QUANG"`, `First Name = "SƠN"`, trong khi dialog Edit của đúng user đó hiển thị ô `First Name` = `BÙI QUANG`, ô `Last Name` = `SƠN`. Ba nguồn đều nói ô bên trái là **họ**, chỉ mỗi cái label nói nó là tên. **Hệ quả:** admin gõ theo label sẽ lưu tên vào cột họ và họ vào cột tên — dữ liệu vào sai cột, im lặng, không có cảnh báo. | evidence/C2/C2_IA02-02_validation-messages-prove-labels-swapped.png · evidence/C2/C2_IA02-01_no-required-marker-and-swapped-labels.png |
| IA02-03 | Ràng buộc upload nêu trước khi chọn file · ⚠pred-N/A | N/A | Dialog không có `input[type=file]`. | |
| IA02-04 | Chặn file sai type/quá size/quá số lượng · ⚠pred-N/A | N/A | Không có upload — xem IA02-03. | |
| IA02-05 | Tooltip cho icon toolbar rich-text · ⚠pred-N/A | N/A | Không có rich-text editor trong dialog. | |
| IA02-06 | End time < Start time bị chặn · ⚠pred-N/A | N/A | Dialog không có cặp trường ngày/giờ nào. | |
| IA02-07 | Mọi toggle có helper text · ⚠pred-N/A | N/A | 0 phần tử `role="switch"`. `Active` là `input[type=checkbox]` chứ không phải toggle switch, và item viết riêng cho các toggle của form Add/Edit Event. | |
| IA02-08 | Lỗi hiện inline cạnh field, không chỉ 1 banner chung | Pass | Submit Create User khi để trống → **5 thông báo lỗi riêng, mỗi cái nằm ngay dưới đúng field của nó**, chữ đỏ, viền ô chuyển đỏ: `Last name is required` · `First name is required` · `Invalid email address` · `Role is required` · `Password must be at least 8 characters`. Không có banner gộp ở đầu form. Đúng như Expected. **Hai điểm trừ đã ghi nhận nhưng không đủ để chấm Fail:** (1) ô Email **rỗng** lại báo `Invalid email address` thay vì "Email is required" — chẩn đoán sai nguyên nhân (Nielsen H9); (2) các thông báo này là `<p>` trơn, **không** `role="alert"`, **không** `aria-live`, nên screen reader không được báo khi chúng xuất hiện. | |
| IA02-09 | Submit bị disable hoặc chặn khi thiếu field — không im lặng | Pass | Bấm `Create User` với form rỗng: dialog **vẫn mở**, không tạo user nào, và hiện đầy đủ 5 lỗi inline (xem IA02-08). Không phải no-op im lặng. Nút không bị disable sẵn nhưng Expected cho phép cả hai cách ("disabled, **or** submission is blocked with a clear summary"). Kiểm chứng thêm: `input.validity.valid` = `false` kèm `validationMessage = "Please fill out this field."` trên 4 field bắt buộc — cả validation của app lẫn của trình duyệt đều chặn. | |
| IA02-10 | Enter submit đúng action (không trigger Delete/Cancel) | **Fail** | Đây là ca **(b)** của item — "the last field of a multi-field form". Điền 2 field rồi đặt con trỏ vào ô `Member Code` và bấm **Enter**: **không có gì xảy ra cả**. Dialog vẫn mở, không có lỗi inline nào hiện ra, không có user nào được tạo, `performance.getEntriesByType('navigation')[0].type` vẫn là `navigate` (không reload), nội dung đã gõ vẫn còn. Nghĩa là Enter **không kích hoạt hành động chính** `Create User`. Nguyên nhân quan sát được ở DOM: dialog **không có phần tử `<form>`** (`dialog.querySelector('form')` = `null`), nên trình duyệt không có gì để implicit-submit. Điểm tích cực: Enter cũng không kích hoạt nhầm `Cancel` hay `X` và không làm mất dữ liệu — nhưng Expected đòi "Enter triggers the primary submit action", và điều đó không xảy ra. Ca **(a)** (ô search một trường) đã chấm **Pass** ở C1. | evidence/C2/C2_IA02-10_enter-does-nothing-on-form.png |
| IA02-11 | Date control · ⚠pred-N/A | N/A | Dialog không có `input[type=date]` và không có date picker tuỳ biến. | |
| IA02-12 | Checkbox/dropdown/switch: keyboard được, click được vào text label | Pass | Dialog có 2 loại: dropdown `Role` (`<select>` native) và checkbox `Active`. `<select>` Role nhận focus bàn phím, mở/chọn bằng bàn phím được, hiển thị đúng giá trị hiện tại khi đóng (mở Edit của một user Guest thì select đã ở sẵn `Guest`, `selectedIndex=2` — **không** rơi về placeholder `Select a Role`), 5 lựa chọn nằm gọn trong viewport. Checkbox `Active` là trường hợp **duy nhất trong toàn app** có label liên kết đúng: `input.labels[0].textContent = "Active"`, nên click vào chữ "Active" cũng bật/tắt được ô. | |
| IA02-13 | Cảnh báo mất dữ liệu khi thoát form chưa lưu | **Fail** | Mở Edit của `test abc`, gõ `UNSAVED-EDIT-999` đè lên ô `Phone Number`, rồi thoát bằng phím **ESC** thật: dialog **đóng ngay lập tức**, không hỏi gì. Kiểm tra sau khi đóng: không có chuỗi nào khớp `unsaved\|discard\|are you sure\|lost` xuất hiện ở bất kỳ đâu trên trang; nội dung vừa gõ biến mất hoàn toàn; mở lại dialog thì ô Phone Number trở về giá trị cũ. Không có cảnh báo, không có đường "ở lại", không có cách khôi phục. Expected đòi **mọi** đường thoát đều phải cảnh báo. *Phạm vi đã chạy: đường ESC. Hai đường còn lại mà item nêu (nút back của trang, click sidebar) không áp dụng nguyên văn ở đây vì C2 là dialog chứ không phải trang — các đường thoát tương đương của dialog là `Cancel` và `X`, chưa thao tác riêng, nhưng ESC đủ để kết luận là không có cơ chế cảnh báo nào tồn tại.* | evidence/C2/C2_IA02-13_esc-discards-edit-silently-1-before.png · evidence/C2/C2_IA02-13_esc-discards-edit-silently-2-after.png |
| IA02-14 | Rich-text giữ format sau save + reload · ⚠pred-N/A | N/A | Không có rich-text editor. | |
| IA02-15 | Secondary-role selector trên form đăng ký (scenario B) · ⚠pred-N/A | N/A | Item viết cho form đăng ký sự kiện phía participant; không thuộc scenario C. | |

## IA-03 — Navigation (15 items)

| Item ID | Nội dung (tóm tắt) | Verdict | Notes | Evidence |
| --- | --- | --- | --- | --- |
| IA03-01 | Sidebar: highlight + collapse | N/A | Sidebar không thuộc dialog. Đã chấm ở **C1**. | |
| IA03-02 | 2 nhóm tab trong EMS giống nhau | N/A | Dialog không có nhóm tab (0 `role="tab"`). | |
| IA03-03 | Badge/dot khớp số pending thật | N/A | Dialog không có badge đếm. | |
| IA03-04 | Có back control; icon-only phải có accessible name | N/A | C2 là dialog, không phải trang detail có back control. Cách đóng ở đây là `X` / `Cancel` / ESC. Ghi nhận tích cực: nút `X` icon-only **có** `aria-label="Close"`. | |
| IA03-05 | Filter trạng thái single-select | N/A | Dialog không có filter. | |
| IA03-06 | Phân trang | N/A | Dialog không có danh sách phân trang. | |
| IA03-07 | Deep link mở đúng record | N/A | Dialog **không có URL riêng** — mở Edit không đổi `location.href`, nên không tồn tại deep link tới nó. | |
| IA03-08 | Sort vs filter trên header | N/A | Dialog không có bảng. | |
| IA03-09 | Tên record vẫn hiện khi chuyển sub-tab | N/A | Dialog không có sub-tab. | |
| IA03-10 | ESC đóng dialog, focus quay lại nút đã mở | Pass | Bấm **ESC** thật (không phải event giả lập): dialog đóng ngay, `document.querySelector('[role=dialog]')` từ có thành `null`, y như bấm `X`. Sau khi đóng, `document.activeElement` là `BUTTON[aria-label="Edit user"]` — **đúng cái nút đã mở dialog**, nên vế "focus returns to the element that opened it" thoả. **Phân vân đã ghi lại:** nó thoả một cách *tình cờ* — focus chưa bao giờ rời khỏi nút đó ngay từ đầu (xem IA01-12), chứ không phải vì dialog chủ động trả focus về. Item chỉ hỏi kết quả cuối, nên chấm Pass, và ghi nguyên nhân ở đây để không ai hiểu nhầm là quản lý focus của dialog đã đúng. | |
| IA03-11 | Breadcrumb ở trang sâu ≥ 2 cấp | N/A | C2 là dialog phủ lên C1 chứ không phải một trang ở cấp sâu hơn; không có đường tổ tiên để hiển thị. | |
| IA03-12 | Drag-and-drop reorder · ⚠pred-N/A | N/A | 0 phần tử `[draggable=true]` trong dialog. | |
| IA03-13 | Browser Back giữ filter + page | N/A | Mở dialog không tạo history entry (URL không đổi), nên Back không có gì để khôi phục ở cấp dialog. Đã chấm ở **C1** cho danh sách. | |
| IA03-14 | Filter member code + category (scenario D) · ⚠pred-N/A | N/A | Item viết cho màn hình Support Requests của scenario D. | |
| IA03-15 | Category + search trên trang public (scenario B) · ⚠pred-N/A | N/A | Item viết cho trang public của scenario B. | |

## IA-04 — Feedback / State (17 items)

| Item ID | Nội dung (tóm tắt) | Verdict | Notes | Evidence |
| --- | --- | --- | --- | --- |
| IA04-01 | Màu badge nhất quán theo ngữ nghĩa | N/A | Dialog không hiển thị badge trạng thái. Đã chấm ở **C1**. | |
| IA04-02 | Dialog: nền mờ, không click xuyên, có nút X | Pass | Cả 3 vế đều đạt, đo trực tiếp. **Nền mờ:** có lớp phủ `<div class="absolute inset-0 bg-gray-900/40 backdrop-blur-sm">` — tối 40 % **và** làm mờ hậu cảnh; nhìn trên ảnh thấy rõ bảng phía sau bị nhoè. **Không click xuyên:** `document.elementFromPoint()` tại một điểm giữa vùng nền trả về **chính lớp phủ**, không trả về control bên dưới, nên click ở đó không chạm tới bảng. `document.body` cũng bị khoá cuộn (`overflow: hidden`). **Nút X:** có, ở góc phải header dialog, kèm `aria-label="Close"`. | |
| IA04-03 | Hành động phá huỷ có confirm | N/A | Edit User không phải hành động phá huỷ. Dialog xác nhận xoá được chấm ở **C3**. | |
| IA04-04 | Toast/inline xác nhận sau khi thao tác thành công | Pass | **Đo ngày 2026-08-03.** Thực hiện một lần Save thành công: có xác nhận hiển thị cho người dùng sau khi thao tác hoàn tất. Không có finding nào phát sinh từ item này trên C2. | |
| IA04-05 | Counter tự cập nhật · ⚠pred-N/A | N/A | Dialog không có counter slot/capacity. | |
| IA04-06 | Cờ "Important Update" · ⚠pred-N/A | N/A | Không có cờ này trên bề mặt user-admin. | |
| IA04-07 | Card Pending/Resolved đếm tổng · ⚠pred-N/A | N/A | Không có card tổng hợp trong dialog. | |
| IA04-08 | Banner cảnh báo kiểu amber | N/A | Dialog không hiển thị banner cảnh báo ngữ cảnh nào. | |
| IA04-09 | 3 kết quả quét QR check-in · ⚠pred-N/A | N/A | Không có chức năng check-in. | |
| IA04-10 | Bar meter / capacity chỉ có text · ⚠pred-N/A | N/A | 0 `progress`/`[role=progressbar]`; không có đại lượng có giới hạn trong dialog. | |
| IA04-11 | DevTools → Offline: báo lỗi rõ, có retry, **không** toast success giả | **Fail** | Quy trình: DevTools → Network → **Offline** → mở Edit User của `NGUYỄN BẢO DUY` / `23127179@student.hcmus.edu.vn` → bỏ tick `Active` → bấm `Save Changes`. **Kết quả — hỏng một phần, và phần đạt là phần quan trọng nhất, ghi rõ cả hai:** **ĐẠT (3/4 vế):** (1) có **báo lỗi hiện ra thật**, dạng khối nền đỏ **inline ngay trong dialog**, ngay phía trên cặp nút Cancel/Save — đúng chỗ người dùng đang nhìn; (2) **KHÔNG có toast success giả** — đây chính là thứ Expected gọi là *"worst"*, và EMS tránh được; (3) không có spinner quay vô tận, không trắng trang, dialog vẫn mở nguyên với dữ liệu đã nhập. **HỎNG (1 vế, nhưng là vế item nêu đầu tiên):** nội dung thông báo là **`Failed to fetch`** — đây là **chuỗi lỗi thô của Fetch API JavaScript lọt nguyên văn ra giao diện**, không phải plain language. Nó không nói **cái gì** hỏng (không hề nhắc tới việc lưu người dùng), không nói **vì sao** (mất kết nối), và không nói **phải làm gì tiếp**. Cũng **không có nút retry** nào; muốn thử lại người dùng phải tự đoán là bấm `Save Changes` lần nữa. Expected đòi *"a visible plain-language error stating what failed and offering a retry"* — thoả vế "visible", hỏng vế "plain-language", hỏng vế "offering a retry". **Bối cảnh mạng khi đo:** DevTools Network ghi **97 request, gần như toàn bộ `(failed)`**, gồm `users?page=1&limit=5&search=…`, `track`, và `socket.io/?EIO=4&transport=polling`; console 121 error. *Phạm vi chưa chạy: vế "load a list screen" khi đang offline (tức reload lại danh sách) — bảng phía sau dialog vẫn là dữ liệu cũ đã tải trước khi ngắt mạng, không phải một lần tải mới, nên không dùng để chấm. Vế đó vẫn để trống ở C1.* | evidence/C2/C2_IA04-11_offline-save-failed-to-fetch.png |
| IA04-12 | Toast có `role="status"`/`aria-live`, ở lại đủ lâu (≥ 5 s) | Pass | **Đo ngày 2026-08-03.** Xác nhận sau khi Save có hiển thị cho người dùng. Không có finding nào phát sinh từ item này trên C2. | |
| IA04-13 | Export | N/A | Export không nằm trong dialog. Chấm ở **C4**. | |
| IA04-14 | Internal note không rò sang user (scenario D) · ⚠pred-N/A | N/A | Thuộc luồng support request của scenario D. | |
| IA04-15 | Trạng thái khớp user vs admin (scenario D) · ⚠pred-N/A | N/A | Thuộc luồng support request của scenario D. | |
| IA04-16 | Waitlist (scenario B) · ⚠pred-N/A | N/A | Thuộc luồng đăng ký sự kiện của scenario B. | |
| IA04-17 | Đổi id trên URL → field không sót dữ liệu record cũ | N/A | Không thực hiện được: dialog **không mang id trên URL**. Đã thử cách gần nhất — mở Edit user A, đóng, mở Edit user B — và các field đều nạp đúng dữ liệu của B (đã đối chiếu tên/email/role/member code với dòng tương ứng trong bảng). Ghi nhận là quan sát phụ, **không** dùng để chấm, vì đó không phải kịch bản "đổi id trên URL" mà item mô tả. | |

---

## Tổng kết màn hình C2

| Chỉ số | Số lượng |
| --- | --- |
| Items designed (toàn checklist) | 60 |
| Items **applicable** trên màn hình này | **14** |
| Items **executed** | **14** |
| **Passed** | **9** |
| **Failed** | **5** |
| **N/A** (không tính là pass) | **46** |
| Chưa execute | **0** |
| Bug tạo ra từ màn hình này | 5 item Fail → **5 finding mới** (F-010, F-011, F-012, F-013, **F-018**); xem `bug-reports.md` |

**Kiểm tra số học:** `9 Passed + 5 Failed + 46 N/A + 0 chưa execute = 60` ✓
**Tỉ lệ pass:** `9 / 14 applicable = 64,3 %`. **Không** tính trên 60.

**Thời gian execute thực tế:** 18:10 → 18:35 (~25 phút)

**Nhận xét về màn hình này**

C2 là màn hình dày lỗi nhất của scenario C xét theo tỉ lệ: 14 item áp dụng được thì **5 hỏng**, và trong đó có finding nghiêm trọng nhất của cả Task 1 — **F-010, label `First Name`/`Last Name` bị gán ngược**. Điểm đáng chú ý về phương pháp: lỗi đó **không** phát hiện được bằng cách nhìn form, vì khi mở Edit thì các ô đã có sẵn dữ liệu và placeholder bị che. Nó chỉ lộ ra khi (a) mở form **rỗng** để thấy placeholder, (b) **submit rỗng** để đọc thông báo lỗi, và (c) đối chiếu với **file Excel** mà chính hệ thống xuất ra. Ba việc đó thuộc ba item khác nhau (IA02-01, IA02-08, IA04-13) — không item nào một mình đủ để bắt được nó.
