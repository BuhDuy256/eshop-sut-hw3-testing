# Checklist Execution — Screen C4 (Export to Excel — xuất file từ Users Management)

**Scenario:** C — Admin manages users
**Người thực hiện:** Nguyễn Bảo Duy — 23127179 — Group 09
**Checklist:** `hw3/docs/gui-checklist/Shared_GUI_Checklist.md` v1.9 — 60 items (frozen)
**SUT:** https://prod-dev.ems-fitus.cloud/ · account `admin@gmail.com`
**URL màn hình:** `https://prod-dev.ems-fitus.cloud/dashboard/admin/users` — Export là nút trên toolbar, không có route riêng
**Đường đi:** C1 → toolbar → nút **Export** (nền xanh lá, icon download)
**Ngày execute:** 2026-08-02
**Browser:** Chrome (Windows 11) qua extension `claude-in-chrome`

---

> ### C4 là một chức năng, không phải một trang — và đó là lý do gần như mọi item đều N/A
>
> §5 của đề gọi tên C4 trực tiếp: *"Export to Excel — column completeness and download feedback"*. Bề mặt của nó gồm đúng ba thứ: **một nút**, **quãng thời gian giữa lúc bấm và lúc file về**, và **nội dung file `.xlsx`**. Không có bảng riêng, không có form, không có dialog, không có điều hướng. Vì vậy **57/60 item không có widget để bám vào** và chỉ **3 item** áp dụng được — nhưng một trong ba chính là **IA04-13**, item mà §5 viết riêng cho màn hình này.
>
> **Chỉ chấm được 1 trong 4 export của EMS.** IA04-13 nói EMS có 4 chỗ export (Users Management · Support requests · `/profile` → My Activities · tab Registrants của một sự kiện). Bộ màn hình của scenario C chỉ chứa **Export trên Users Management**. Ba chỗ còn lại thuộc màn hình của thành viên khác — **không chấm**, và điều đó được ghi ở đây thay vì lặng lẽ mở rộng kết luận ra cả 4.

**Cách đọc file:** file `.xlsx` được mở bằng Python (`zipfile` + đọc `xl/worksheets/sheet1.xml`, `xl/sharedStrings.xml`, `xl/styles.xml`), không qua Excel. Cách này đọc được cả **giá trị thô lẫn định dạng ô**, nên phân biệt được "ngày lưu thành số serial nhưng có numFmt nên hiển thị đúng" với "ngày hỏng thật" — xem IA04-13.

---

## IA-01 — General UI Standards (13 items)

| Item ID | Nội dung (tóm tắt) | Verdict | Notes | Evidence |
| --- | --- | --- | --- | --- |
| IA01-01 | Consistency: page-title, padding card, KPI card | N/A | Export là một nút trên toolbar của C1, không phải trang có tiêu đề/card riêng. Đã chấm ở **C1**. | |
| IA01-02 | 9 icon sidebar cùng stroke/size | N/A | Không liên quan tới chức năng export. Đã chấm ở **C1**. | |
| IA01-03 | Màu primary chỉ dùng cho CTA chính | N/A | Nút Export dùng xanh lá, nút primary cyan là `Add User`; cả hai nằm trên toolbar của C1 và đã được đánh giá cùng nhau ở **C1**. Không chấm lại để tránh đếm hai lần. | |
| IA01-04 | Section header khác biệt với field label | N/A | Không có form/section trong luồng export. | |
| IA01-05 | Contrast ≥ 4.5:1 trên status pill | N/A | Luồng export không hiển thị status pill nào. Đã đo ở **C1**. | |
| IA01-06 | Empty state | N/A | Không có danh sách nào trong luồng export có thể rỗng. | |
| IA01-07 | Loading state (Slow 3G) | N/A | Item yêu cầu **reload một màn hình nhiều dữ liệu**; export không tải lại màn hình. Vấn đề "không có busy state khi bấm Export" thuộc **IA04-13** và đã chấm ở đó. Ca cần throttling để lại ở **C1**. | |
| IA01-08 | Toggle EN/VI dịch hết label | N/A | Control ngôn ngữ ở header trang, đã chấm ở **C1**. Ghi nhận: **nội dung file xuất ra lại trộn hai ngôn ngữ** — nhưng đó là nội dung file, thuộc IA04-13, không phải toggle. | |
| IA01-09 | Định dạng ngày/số theo locale | N/A | Đã chấm ở **C1** cho giao diện. Ngày **bên trong file** thuộc IA04-13. | |
| IA01-10 | Spotlight hero · ⚠pred-N/A | N/A | Không có hero. | |
| IA01-11 | QR/barcode · ⚠pred-N/A | N/A | Không có QR/barcode. | |
| IA01-12 | Focus ring rõ khi Tab | N/A | Nút Export nằm trong thứ tự Tab của C1 và **đã được đo ở đó** (nó là một trong các phần tử có ring nhìn rõ: `outline auto rgb(255,255,255)`). Không chấm lại. | |
| IA01-13 | Ảnh có `alt` mô tả | N/A | Luồng export không có `<img>`; icon download là SVG đi kèm chữ "Export". | |

## IA-02 — Forms (15 items)

> Luồng export **không có field nhập liệu nào**: bấm nút là tải file, không có dialog chọn cột / chọn phạm vi / chọn định dạng. Toàn bộ IA-02 vì vậy không áp dụng. *(Việc **không có** hộp thoại chọn phạm vi lại chính là một phần nguyên nhân của lỗi IA04-13 — nhưng nó được chấm ở đó, không tách thành item form.)*

| Item ID | Nội dung (tóm tắt) | Verdict | Notes | Evidence |
| --- | --- | --- | --- | --- |
| IA02-01 | Đánh dấu required | N/A | Luồng export không có form. → chấm ở **C2**. | |
| IA02-02 | Label không biến mất khi gõ | N/A | Không có field để gõ. → chấm ở **C2**. | |
| IA02-03 | Ràng buộc upload · ⚠pred-N/A | N/A | Export là tải **xuống**, không có upload. | |
| IA02-04 | Chặn file sai type/size · ⚠pred-N/A | N/A | Không có upload. | |
| IA02-05 | Tooltip icon rich-text · ⚠pred-N/A | N/A | Không có rich-text editor. | |
| IA02-06 | End time < Start time · ⚠pred-N/A | N/A | Không có trường ngày giờ trong luồng export. | |
| IA02-07 | Toggle có helper text · ⚠pred-N/A | N/A | Không có toggle. | |
| IA02-08 | Lỗi hiện inline cạnh field | N/A | Không có field để phát sinh lỗi. → chấm ở **C2**. | |
| IA02-09 | Submit bị chặn khi thiếu field | N/A | Export không phải submit form. → chấm ở **C2**. | |
| IA02-10 | Enter submit đúng action | N/A | Không có ô nhập liệu trong luồng export. → chấm ở **C1** và **C2**. | |
| IA02-11 | Date control · ⚠pred-N/A | N/A | Không có date control để giới hạn khoảng export. | |
| IA02-12 | Checkbox/dropdown/switch keyboard | N/A | Luồng export không có control lựa chọn nào — chỉ có một nút. | |
| IA02-13 | Cảnh báo mất dữ liệu khi thoát form | N/A | Không có dữ liệu nhập dở. → chấm ở **C2**. | |
| IA02-14 | Rich-text giữ format · ⚠pred-N/A | N/A | Không có rich-text editor. | |
| IA02-15 | Secondary-role selector (scenario B) · ⚠pred-N/A | N/A | Thuộc scenario B. | |

## IA-03 — Navigation (15 items)

> Export không điều hướng đi đâu cả — không đổi URL, không mở trang mới, không tạo history entry. Toàn bộ IA-03 không áp dụng.

| Item ID | Nội dung (tóm tắt) | Verdict | Notes | Evidence |
| --- | --- | --- | --- | --- |
| IA03-01 | Sidebar highlight + collapse | N/A | Không liên quan tới export. Đã chấm ở **C1**. | |
| IA03-02 | 2 nhóm tab giống nhau | N/A | Luồng export không có tab. | |
| IA03-03 | Badge khớp số pending | N/A | Không có badge trong luồng export. | |
| IA03-04 | Back control có accessible name | N/A | Export không dẫn tới trang con nào nên không cần back control. | |
| IA03-05 | Filter trạng thái single-select | N/A | Filter thuộc bảng C1 và đã chấm ở đó. *(Việc export **phớt lờ** filter đang bật được chấm ở **IA04-13**, không phải ở đây.)* | |
| IA03-06 | Phân trang | N/A | Export không phân trang. Đã chấm ở **C1**. | |
| IA03-07 | Deep link mở đúng record | N/A | Export không có URL để deep-link. | |
| IA03-08 | Sort vs filter trên header | N/A | Thuộc bảng C1, đã chấm ở đó. | |
| IA03-09 | Tên record hiện khi chuyển sub-tab | N/A | Không có sub-tab. | |
| IA03-10 | ESC đóng dialog | N/A | Export không mở dialog nào. → chấm ở **C2**/**C3**. | |
| IA03-11 | Breadcrumb | N/A | Export không tạo cấp điều hướng mới. | |
| IA03-12 | Drag-and-drop reorder · ⚠pred-N/A | N/A | Không có danh sách sắp xếp được. | |
| IA03-13 | Browser Back giữ filter + page | N/A | Export không tạo history entry. Đã chấm ở **C1**. | |
| IA03-14 | Filter member code + category (scenario D) · ⚠pred-N/A | N/A | Thuộc scenario D. | |
| IA03-15 | Category + search trang public (scenario B) · ⚠pred-N/A | N/A | Thuộc scenario B. | |

## IA-04 — Feedback / State (17 items)

| Item ID | Nội dung (tóm tắt) | Verdict | Notes | Evidence |
| --- | --- | --- | --- | --- |
| IA04-01 | Màu badge nhất quán theo ngữ nghĩa | N/A | Luồng export không hiển thị badge. Đã chấm ở **C1**. | |
| IA04-02 | Dialog: nền mờ, không click xuyên, có X | N/A | Export không mở dialog. → chấm ở **C2**/**C3**. | |
| IA04-03 | Hành động phá huỷ có confirm | N/A | Export không phải hành động phá huỷ. → chấm ở **C3**. | |
| IA04-04 | Toast/inline xác nhận sau khi thao tác thành công | **Fail** | Export **là** một hành động hoàn tất thành công (file về thật, 14 339 bytes) nhưng giao diện **không nói một chữ nào**. Đo bằng polling `getComputedStyle`/thuộc tính nút mỗi **150 ms trong 6 giây** kể từ lúc click: `button.disabled` = `false` ở **mọi** mẫu; `innerText` luôn là `Export`; không có phần tử `.animate-spin` nào bên trong nút; không có toast, không có dòng chữ xác nhận, không có thay đổi nào trên trang. Người dùng chỉ biết export thành công nếu tự đi mở thư mục Downloads. Trùng nguyên nhân gốc với **F-014** (không có hệ thống toast trong toàn app) nhưng biểu hiện ở luồng khác, nên vẫn ghi Fail tại đây và gộp finding ở `bug-reports.md`. | evidence/C4/C4_IA04-13_export-no-feedback-and-ignores-filter.png |
| IA04-05 | Counter tự cập nhật · ⚠pred-N/A | N/A | Luồng export không có counter. | |
| IA04-06 | Cờ "Important Update" · ⚠pred-N/A | N/A | Không có cờ này. | |
| IA04-07 | Card Pending/Resolved đếm tổng · ⚠pred-N/A | N/A | Không có card tổng hợp trên Users Management. | |
| IA04-08 | Banner cảnh báo kiểu amber | N/A | Luồng export không hiện banner cảnh báo nào — kể cả khi export bỏ qua filter đang bật, chỗ mà một banner sẽ có ích (ghi nhận này thuộc IA04-13). | |
| IA04-09 | 3 kết quả quét QR check-in · ⚠pred-N/A | N/A | Không có chức năng check-in. | |
| IA04-10 | Bar meter khớp số / capacity chỉ có text · ⚠pred-N/A | N/A | Không có đại lượng có giới hạn nào được hiển thị trong luồng export; 0 `progress`/`[role=progressbar]`. Item có nhắc "any upload or **Export indicator**" — nhưng ở đây **không có indicator nào tồn tại** để so fill với số, và việc thiếu hẳn indicator đã được chấm ở **IA04-04** và **IA04-13**, nên không chấm lần thứ ba. | |
| IA04-11 | DevTools → Offline · | N/A | Ca offline được để lại cho luồng **Save** ở **C2** (nơi câu hỏi "có toast success giả không" có ý nghĩa nhất). Bấm Export khi offline chỉ cho biết file không về — ít thông tin hơn, và item chỉ cần chạy một lần trên một luồng ghi. | |
| IA04-12 | Toast có `role="status"`/`aria-live`, ở lại đủ lâu | **Fail** | Không có toast để đo thời lượng (xem IA04-04). Đo phần còn lại: `document.querySelectorAll('[aria-live],[role=status],[role=alert]')` = **0** phần tử, trước khi bấm Export, trong 6 giây sau khi bấm, và sau khi file đã về. Với WCAG 2.1 SC 4.1.3, việc **file đã tải xong** hoàn toàn không được thông báo cho người dùng screen reader — họ không có cách nào biết hành động đã hoàn tất, vì đây là luồng duy nhất mà phản hồi *chỉ* nằm ngoài trang (trong thư mục Downloads). | evidence/C4/C4_IA04-13_export-no-feedback-and-ignores-filter.png |
| IA04-13 | Export: có feedback, tên file có nghĩa, đủ cột, rõ phạm vi filter | **Fail** | Item chính của màn hình. Hỏng ở **4 vế độc lập**; vế còn lại đạt. **(1) Feedback — KHÔNG CÓ GÌ.** Xem IA04-04: nút không disable, không spinner, không toast, suốt 6 s đo ở nhịp 150 ms. Expected: *"never a dead button with no response"*. **(2) Tên file — không đọc được.** File về tên `users-export-1785667505695.xlsx`. `1785667505695` là **epoch tính bằng mili-giây**, không phải ngày người đọc được. Expected đòi *"a meaningful, dated filename"*. **(3) Phạm vi export — SAI, và im lặng.** Đây là vế nặng nhất. Lúc bấm Export, màn hình đang lọc bằng search `abc` và hiển thị **đúng 1 dòng**. File tải về chứa **105 dòng dữ liệu** — toàn bộ dataset; user đang được lọc nằm ở dòng thứ **38**. Giao diện **không hề** nói export lấy toàn bộ hay lấy phần đang lọc. Expected ghi thẳng: *"silently exporting something other than what the user is looking at is a Fail"*. **(4) Cột — thiếu và đổi tên.** Header trong file: `Last Name · First Name · Email · Phone · Card Code · Role · Status · Created At`. Bảng trên màn hình: `USER · Role · MEMBER CODE · Status · CREATED · UPDATED`. → cột **`UPDATED` thiếu hẳn** khỏi file, và `MEMBER CODE` bị đổi tên thành **`Card Code`**, không khớp thuật ngữ trên UI. **(5) Ngôn ngữ trong file.** Giá trị cột `Status` được ghi bằng **tiếng Việt** (`Hoạt động`) trong khi toàn bộ giao diện đang là tiếng Anh (`Active`) và không có cách nào đổi ngôn ngữ (F-001). **Vế ĐẠT, ghi lại để không thổi phồng:** ngày trong cột `Created At` lưu ở dạng số serial Excel (`46236.42448041667`) **nhưng ô có định dạng `numFmt 164 = "DD/MM/YYYY HH:MM"`** — kiểm trong `xl/styles.xml` — nên mở bằng Excel sẽ hiển thị đúng là ngày giờ. **Không** báo đây là lỗi. | evidence/C4/C4_IA04-13_export-no-feedback-and-ignores-filter.png · evidence/C4/C4_IA04-13_file-contents-vs-screen.txt |
| IA04-14 | Internal note không rò sang user (scenario D) · ⚠pred-N/A | N/A | Thuộc scenario D. | |
| IA04-15 | Trạng thái khớp user vs admin (scenario D) · ⚠pred-N/A | N/A | Thuộc scenario D. | |
| IA04-16 | Waitlist (scenario B) · ⚠pred-N/A | N/A | Thuộc scenario B. | |
| IA04-17 | Đổi id trên URL → không sót dữ liệu record cũ | N/A | Export không có id trên URL và không phải trang detail. | |

---

## Tổng kết màn hình C4

| Chỉ số | Số lượng |
| --- | --- |
| Items designed (toàn checklist) | 60 |
| Items **applicable** trên màn hình này | **3** |
| Items **executed** | **3** |
| **Passed** | **0** |
| **Failed** | **3** |
| **N/A** (không tính là pass) | **57** |
| Bug tạo ra từ màn hình này | 3 item Fail → **5 finding** (F-016 … F-020), vì riêng IA04-13 tách thành 4 finding nguyên nhân gốc khác nhau; xem `bug-reports.md` |

**Kiểm tra số học:** `0 Passed + 3 Failed + 57 N/A = 60` ✓
**Tỉ lệ pass:** `0 / 3 applicable = 0 %`.

**Thời gian execute thực tế:** 17:45 → 18:00 (~15 phút, gồm cả việc mở và phân tích file `.xlsx`)

**Nhận xét về màn hình này**

C4 là màn hình **duy nhất không có item nào Pass**. Chỉ 3 item áp dụng được, nhưng cả 3 đều hỏng, và một trong số đó (**IA04-13**) là item mà đề bài §5 viết riêng cho màn hình này.

Điểm đáng nói về phương pháp: **ba trong bốn lỗi của IA04-13 chỉ lộ ra sau khi mở file `.xlsx` ra đọc.** Nếu chỉ dừng ở "bấm Export → file về → coi như xong" thì chỉ bắt được lỗi (1) thiếu feedback và (2) tên file xấu. Lỗi nghiêm trọng nhất — **export bỏ qua filter đang bật** — chỉ phát hiện được khi cố tình bấm Export **trong lúc đang lọc** rồi **đếm số dòng trong file** (1 dòng trên màn hình so với 105 dòng trong file). Đây đúng là hai việc mà runbook Step 3.4 cảnh báo là "chỗ hay bị bỏ qua nhất".

Cùng lúc, việc mở file ra đọc cũng **ngăn được một báo cáo sai**: giá trị ngày trong file trông như số vô nghĩa (`46236.42448041667`), rất dễ kết luận vội là lỗi định dạng ngày. Kiểm `xl/styles.xml` cho thấy ô có `numFmt` đúng nên Excel hiển thị bình thường — nên vế đó được ghi là **đạt**, không phải lỗi.
